# 00. StellarNet Lite 文档总览与学习路径（精简重构版）

你提出的 7 类问题，当前不通过“新增文档”解决，而是通过“重排现有文档入口”解决。  
目标是：文档总数不增加，但定位效率提升。

---

## 0. 当前文档分层目录

`Doc` 下目前按用途分三层：

1. `00-总览`
   放导航与学习路径（当前文档）。
2. `10-功能说明`
   放上手、功能、实战、扩展指南（`01~13` 与 `07A/07B/07C`）。
3. `20-源码剖析`
   放源码索引与逐层参考（`14~17`）。

这样做的目标是：新手先看“功能说明”，二开或排错再进入“源码剖析”。

---

## 1. 你关心的 7 个主题，对应现有文档

### 1.1 框架所有文件夹目录说明和关联

主读：

1. `14. StellarNet Lite 源码总索引与阅读地图`
2. `15. StellarNet Lite Runtime 源码逐层参考`
3. `16. StellarNet Lite Extensions 源码逐层参考`
4. `17. StellarNet Lite Editor 与 LoadTest 源码参考`

### 1.2 框架所有脚本说明和关联

主读：

1. `15`
2. `16`
3. `17`

### 1.3 每个脚本中每个字段/函数说明和关联

主读：

1. `15`
2. `16`
3. `17`

补充：

1. `07A`（Transform 同步字段/函数）
2. `07B`（Animator 同步字段/函数）
3. `07C`（ObjectSync 双端链路字段/函数）

### 1.4 内置模块和业务组件使用说明（登录/大厅/房间/房间业务组件）

主读：

1. `02. 核心概念与全流程指南`
2. `04. 全局模块与房间组件开发指南`
3. `16`（DefaultGameFlow / RoomFlow 的源码级职责）

### 1.5 内置模块/组件拦截扩展说明

主读：

1. `09. 回放重连与框架自定义指南`
2. `10. 零修改框架实现原功能扩展指南`
3. `16`（RuntimeFeatureBridge / Module / RoomComponent 接入点）

### 1.6 模块组件扩展说明

主读：

1. `03. 30分钟业务开发上手实践`
2. `04`
3. `05. 代码生成与编辑器工具链指南`
4. `10`

### 1.7 从启动器到完整实体同步实战（启动/登录/建房/实体同步双端）

主读顺序：

1. `01. 首次运行与 SocialDemo 跑通指南`
2. `02`
3. `04`
4. `07`
5. `07A`
6. `07B`
7. `07C`

---

## 2. 精简后的推荐阅读路径

### 2.1 新手上线路径（先能做，再懂原理）

1. `01`
2. `02`
3. `03`
4. `04`
5. `07 -> 07A -> 07B -> 07C`

### 2.2 深度二开路径（先看结构，再看实现）

1. `14`
2. `15`
3. `16`
4. `17`
5. `10`

---

## 3. 文档列表（去冗余版）

只保留现有文档集合（`01~17` + `07A/07B/07C`），每篇只承担一个主职责：

1. `01`：首次运行与 SocialDemo 跑通
2. `02`：核心概念与完整流程
3. `03`：30 分钟快速上手实践
4. `04`：全局模块与房间组件边界 + 闭环实操
5. `05`：协议/组件常量生成与编辑器工具链
6. `06`：传输层（TCP/KCP/UDP）与通信层说明
7. `07`：ObjectSync 总览
8. `07A`：Transform 同步专项
9. `07B`：Animator 同步专项
10. `07C`：ObjectSync 双端模板实操
11. `08`：UI 路由与表现层解耦
12. `09`：回放与重连
13. `10`：零修改框架扩展
14. `11`：服务端异步持久化工具
15. `12`：无头模式服务端命令
16. `13`：房间多线程调度与分片
17. `14`：源码阅读地图
18. `15`：Runtime 源码逐层参考
19. `16`：Extensions 源码逐层参考
20. `17`：Editor 与 LoadTest 源码参考

---

## 4. 缺失内容补齐状态（不新增文档前提）

已按“只改旧文档”完成本轮补齐：

1. `04` 已补“启动→登录→建房→进房→房间内同步”的闭环步骤与最小模板骨架
2. `07C` 已补“客户端发什么才会触发同步”“为什么只自定义 C2S 不自定义 S2C”
3. `15` 已补“高频问题定位表”“TCP/KCP 生产自检清单”“逐脚本字段/函数手册（两批）”
4. `16` 已补“默认业务模块协作时序”“ObjectSync 接入核对表”“常见误区”“核心扩展逐脚本手册”
5. `17` 已补“从声明到运行的生成闭环”“生成器与压测排坑表”“Editor+LoadTest 逐脚本手册”
6. 字段/函数级细节持续由 `15/16/17` 与 `07A/07B/07C` 承担，不再新增平行文档

后续迭代原则不变：缺内容就补到对应旧文档，不增加文档编号数量。
