# 15. StellarNet Lite Runtime 源码逐层参考

本文档专门覆盖 `StellarNetLite/Runtime`。

阅读目标不是背 API 名字，而是搞清楚三件事：

1. 运行时主链从哪里进入、在哪里切分客户端和服务端。
2. 每个核心类持有哪些状态、这些状态为什么存在。
3. 某个问题应该去哪个脚本里排查，而不是盲目全文搜索。

---

## 1. Runtime 总体结构

`Runtime` 可以按职责拆成六块：

- `StellarNetAppManager.cs`
  统一入口，负责装配传输、序列化、配置、自动注册表、运行时扩展桥。
- `Client/Core`
  客户端状态机、房间对象、房间组件装配、全局/房间事件、客户端网络门面。
- `Server/Core`
  服务端状态机、会话、房间、快照、分发器、房间组件装配。
- `Server/Infrastructure`
  服务端线程宿主、房间分片调度、异步持久化、统一出站队列、无头控制台。
- `Shared/Core` 与 `Shared/Infrastructure`
  协议特性、包结构、协议索引、传输接口、配置、日志、主线程桥接、序列化。
- `Transports`
  `TCP` / `KCP` / `UDP` 及包编解码器。

---

## 2. 运行时入口层

### 2.1 `Runtime/StellarNetAppManager.cs`

文件职责：

- 整个框架的统一运行时入口。
- 在 `Awake` 中安装 `UnityPlayerLoopDispatcher`、装配传输层、序列化器与配置。
- 负责把自动生成绑定器接入 `ServerRoomFactory` / `ClientRoomFactory`。
- 负责创建 `ClientApp`、`ServerApp` 与 `ServerRuntimeHost`。
- 负责把传输层事件桥接到客户端/服务端逻辑宿主。
- 负责重连协程和运行时扩展桥生命周期。

关键关联：

- 上游依赖 `INetworkTransport`、`NetConfigLoader`、`NetMessageMapper`。
- 下游创建 `ClientApp`、`ServerApp`、`ServerRuntimeHost`。
- 通过 `GeneratedRuntimeAccess` 和 `IRuntimeFeatureBridge` 让扩展层接入而不反向污染 Runtime。

重要字段/属性：

- `Serializer`
  当前序列化器，默认是 `LiteNetSerializer`。
- `Transport`
  当前场景上挂载的物理传输组件。
- `_netConfig`
  当前运行时配置快照。
- `_serverRuntimeRequested`
  是否请求维持服务端运行，用来区分“已停服”和“传输层尚未回调”的时序。
- `_serverLifecycleLock`
  服务端宿主创建/销毁互斥锁，避免重复启动或并发回收。
- `_serverRuntime`
  真实的服务端后台宿主线程。
- `_runtimeFeatureBridges`
  当前所有运行时扩展桥实例。
- `ServerApp`
  当前服务端逻辑宿主。
- `ClientApp`
  当前客户端逻辑宿主。
- `_reconnectCoroutine`
  自动重连协程句柄。

关键函数：

- `Awake()`
  安装主线程桥接器，装配 transport/serializer/config，初始化消息映射器和自动绑定器，注册传输事件。
- `ApplyConfig(NetConfig config)`
  热更新当前运行时配置并同步给传输层。
- `StartClient()/StopClient()/StartServer()/StopServer()/StartHost()`
  对外暴露的物理层快捷代理。
- `EnsureServerRuntimeCreated()`
  真正创建 `ServerApp`、注册未鉴权协议、装配自动模块、创建 `ServerRuntimeHost`。
- `DisposeServerRuntime()`
  停止宿主线程、销毁 `ServerApp`、清空房间工厂注册。
- `HandleServerStarted/Stopped/ClientConnected/...`
  负责把传输层物理事件翻译成 Runtime 层状态变更。
- `HandleServerReceivePacket`
  将服务端收包转交给 `ServerRuntimeHost` 线程。
- `HandleClientReceivePacket`
  客户端收包时先给扩展桥，再交给 `ClientApp`。
- `ReconnectionRoutine()`
  在线房间断线后的 15 秒自动重试循环；期间广播本地事件并定时 `Transport.StartClient()`。
- `RestartReconnectionRoutine()`
  允许扩展或 UI 在挂起态下重置重连窗口。

设计重点：

- `StellarNetAppManager` 不直接处理业务协议，它只负责装配和转发。
- 所有扩展逻辑通过 `IRuntimeFeatureBridge` 插入。
- 服务端逻辑严格收敛到 `ServerRuntimeHost` 线程。

### 2.2 `Runtime/GeneratedRuntimeAccess.cs`

文件职责：

- 作为 Runtime 对自动生成代码的“安全访问层”。
- 当某些生成文件暂时不存在时，避免 Runtime 编译直接炸掉。

类型与成员：

- `GeneratedRuntimeAccess`
  - `RuntimeFeatureRegistryTypeName`
    生成的运行时扩展桥注册表完整类型名。
  - `UnauthenticatedRegistryTypeName`
    未鉴权协议白名单注册表完整类型名。
  - `CreateRuntimeFeatureBridges()`
    反射调用生成注册表的 `CreateFeatures`。
  - `GetUnauthenticatedGlobalC2SMsgIds()`
    反射读取白名单字段 `GlobalC2SMsgIds`。
  - `ResolveType(string fullName)`
    在当前程序集内查找生成类型。

### 2.3 `Runtime/IRuntimeFeatureBridge.cs`

文件职责：

- 定义扩展层接入 Runtime 的统一桥接口。
- Runtime 只认接口，不认扩展具体类型。

类型与成员：

- `IRuntimeFeatureBridge`
  - `OnRuntimeAwake`
    Runtime 宿主 `Awake` 完成时调用。
  - `OnServerAppCreated`
    `ServerApp` 创建完成时调用。
  - `OnServerSessionStateChanged`
    会话在线/离线/绑定变化时调用。
  - `TryNotifyServerSessionKick`
    服务端踢人前让扩展补发业务通知。
  - `OnClientAppCreated`
    `ClientApp` 创建完成时调用。
  - `OnClientConnected`
    客户端物理连接成功时调用。
  - `OnClientDisconnected`
    客户端物理断开时调用。
  - `OnClientPacketReceived`
    客户端收到网络包后调用。
  - `OnClientStopped`
    客户端停止时调用。
- `RuntimeFeatureBridgeBase`
  所有方法默认空实现，减少扩展样板代码。

---

## 3. Client/Core

### 3.1 `Runtime/Client/Core/ClientApp.cs`

文件职责：

- 客户端逻辑主状态机。
- 维护当前客户端状态、会话、当前房间、全局分发器。
- 负责所有 C2S 发包前的合法性检查。

类型：

- `ClientAppState`
  - `InLobby`
    大厅态。
  - `OnlineRoom`
    正式在线房间态。
  - `SandboxRoom`
    本地沙盒或纯回放房间态。
  - `ConnectionSuspended`
    物理连接断开、等待恢复态。
- `ClientApp`

关键状态：

- `Session`
  当前客户端会话对象。
- `GlobalDispatcher`
  全局协议分发器。
- `CurrentRoom`
  当前在线房间或回放房间。
- `State`
  当前状态机状态。
- `IsNetworkBlocked`
  弱网阻断时的发包总闸。
- `_sendSeq`
  客户端单会话内的发送序号。
- `_isCurrentRoomConfirmed`
  是否已经拿到 `RoomSetupResult.Success`。
- `_weakNetBypassMsgIds`
  弱网阻断/挂起态仍允许发送的协议白名单。

关键函数：

- `RegisterWeakNetBypassProtocol(int)` / `RegisterWeakNetBypassProtocol<T>()`
  注册弱网豁免发包协议。
- `Dispose()`
  深度销毁当前客户端逻辑和房间。
- `RegisterDisposeCallback(Action)`
  给扩展提供随 `ClientApp` 一起回收的钩子。
- `ConfirmCurrentRoom(string roomId)`
  标记当前在线房间已最终确认。
- `OnReceivePacket(Packet packet)`
  按 `NetScope` 转给全局分发器或房间分发器。
- `EnterOnlineRoom(string roomId)`
  切入正式在线房间并绑定 `ClientSession`。
- `EnterSandboxRoom(string roomId)`
  切入本地沙盒/回放房间。
- `LeaveRoom(bool silent = false)`
  只清理客户端本地房间上下文，不一定意味着已通知服务端。
- `SuspendConnection()`
  在线房间断线时进入软挂起态。
- `AbortConnection()`
  硬中止会话并回大厅态。
- `SendMessage<T>(T msg)`
  所有客户端发包总入口；会做消息元信息、方向、状态、弱网、房间确认等校验。
- `ExecuteDisposeCallbacks()`
  执行扩展注册的销毁回调。

### 3.2 `Runtime/Client/Core/ClientSession.cs`

文件职责：

- 持有客户端本地会话状态，不负责网络发送。

字段/属性：

- `SessionId`
  登录成功后服务端分配的会话标识。
- `AccountId`
  当前登录账号。
- `CurrentRoomId`
  当前正式所在房间。
- `IsLoggedIn`
  通过 `SessionId` 判断是否已登录。
- `IsPhysicalOnline`
  底层连接是否在线。
- `LastBoundRoomId`
  最近一次成功进入的房间。
- `LastDisconnectRealtime`
  最近一次断开时间。
- `IsReconnecting`
  是否正在执行重连链。

函数：

- `SetAccountId`
  只更新账号字段，通常由上层流程用。
- `OnLoginSuccess`
  刷新登录成功后的会话状态。
- `BindRoom`
  绑定当前房间。
- `UnbindRoom`
  清空当前房间。
- `Clear`
  清空整份会话上下文。
- `ClearRecoveryContext`
  仅清理重连恢复上下文。

### 3.3 `Runtime/Client/Core/ClientRoom.cs`

文件职责：

- 客户端房间实例。
- 管理房间组件、房间协议分发器、房间级事件总线。

字段/属性：

- `RoomId`
  当前房间标识。
- `Dispatcher`
  房间域协议分发器。
- `NetEventSystem`
  房间级本地事件系统。
- `_components`
  当前房间已装配的组件列表。
- `Components`
  供回放或视图层按类型查找组件的只读视图。
- `_isDestroyed`
  防止重复销毁。

函数：

- `Create(string roomId)`
  校验并创建房间实例。
- `AddComponent(ClientRoomComponent component)`
  把组件挂到房间上。
- `GetComponent<T>()`
  通过泛型查找组件。
- `InitializeComponents()`
  依次调用各组件 `OnInit`。
- `Destroy()`
  依次调用各组件 `OnDestroy`，清空分发器和事件系统。

### 3.4 `Runtime/Client/Core/ClientRoomComponent.cs`

文件职责：

- 客户端房间组件基类。

成员：

- `Room`
  组件所属房间，由工厂在装配时注入。
- 典型虚函数：
  实际基类里只保留最小宿主关系，业务组件通过继承扩展自己的 `OnInit/OnDestroy` 和 `[NetHandler]`。

### 3.5 `Runtime/Client/Core/ClientRoomFactory.cs`

文件职责：

- 按 `ComponentId` 创建并装配客户端房间组件。

关键成员：

- `ComponentBinder`
  自动生成的消息处理器绑定器。
- `Registry`
  `ComponentId -> 构造器` 映射表。
- `Register`
  注册组件构造器。
- `Clear`
  清空注册表。
- `BuildComponents`
  按组件 ID 批量创建、挂载、绑定消息并最终初始化。

### 3.6 `Runtime/Client/Core/ClientDispatchers.cs`

文件职责：

- 保存客户端全局域与房间域消息处理表。

类型与成员：

- `ClientGlobalDispatcher`
  - `_handlers`
    `MsgId -> Action<Packet>`。
  - `Register`
    注册全局协议处理器。
  - `Dispatch`
    分发一条全局协议。
  - `Clear`
    清空处理器。
- `ClientRoomDispatcher`
  - `_handlers`
    房间域处理器表。
  - `_roomId`
    用于日志定位。
  - `Register/Dispatch/Clear`
    与全局分发器一致，只是带房间语义。

### 3.7 `Runtime/Client/Core/NetClient.cs`

文件职责：

- 客户端静态网络门面。
- 让业务层在不知道 `ClientApp` 实例引用的情况下也能发消息和查状态。

成员：

- `_app`
  当前绑定的 `ClientApp`。
- `App` / `Session` / `State` / `CurrentRoom`
  常用状态快速访问器。
- `Initialize(ClientApp app)`
  绑定或清空当前客户端主状态机。
- `Send<T>(T msg)`
  静态发包入口，内部转到 `ClientApp.SendMessage`。

### 3.8 `Runtime/Client/Core/Events/GlobalTypeNetEvent.cs`

文件职责：

- 全局强类型事件总线。
- 不是网络协议分发器，而是客户端本地事件系统。

关键类型与成员：

- `GlobalTypeNetEvent`
  - `Register<T>`
    注册某类型事件的回调，返回注销令牌。
  - `UnRegister<T>`
    显式注销单个监听。
  - `Broadcast<T>(T e)` / `Broadcast<T>()`
    广播事件实例或默认实例。
  - `UnRegisterAll<T>`
    清空某事件类型的全部监听。
- `EventBox<T>`
  保存某一种事件类型的订阅者与令牌池。
- `EventToken`
  令牌实现，支持：
  - `UnRegister`
  - `UnRegisterWhenGameObjectDestroyed`
  - `UnRegisterWhenMonoDisable`

### 3.9 `Runtime/Client/Core/Events/IUnRegister.cs`

文件职责：

- 定义注销令牌接口与默认触发器实现。

类型与成员：

- `IUnRegister`
  - `UnRegister`
  - `UnRegisterWhenGameObjectDestroyed`
  - `UnRegisterWhenMonoDisable`
- `CustomUnRegister`
  基于 `Action` 的默认令牌实现。
- `EventUnregisterTrigger`
  `GameObject` 销毁时批量注销。
- `EventUnregisterDisableTrigger`
  `MonoBehaviour` 禁用时批量注销。

### 3.10 `Runtime/Client/Core/Events/RoomNetEventSystem.cs`

文件职责：

- 房间内局部事件总线。
- 作用域比 `GlobalTypeNetEvent` 小，只绑定到某个 `ClientRoom`。

成员：

- `_delegates`
  `Type -> Delegate` 映射。
- `_roomId`
  仅用于日志和定位。
- `Register<T>`
  注册房间事件。
- `UnRegister<T>`
  注销房间事件。
- `Broadcast<T>`
  广播房间事件。
- `Clear`
  清空所有房间事件监听。

### 3.11 `Runtime/Client/Core/Events/LocalEvents/LocalNetEvents.cs`

文件职责：

- 定义 Runtime 自己广播的本地事件结构体。

类型与字段：

- `Local_ConnectionSuspended`
  - `RemainingSeconds`
    剩余自动重连秒数。
- `Local_ReconnectTimeout`
  自动重连超时事件，无字段。
- `Local_SystemPrompt`
  - `Message`
    轻提示文本。
- `Local_ConnectionAborted`
  硬中止连接事件，无字段。

---

## 4. Server/Core

### 4.1 `Runtime/Server/Core/ServerApp.cs`

文件职责：

- 服务端主状态机。
- 持有所有房间、会话、全局模块、未鉴权协议白名单。
- 执行服务端收包、路由、GC、发包入口。

关键状态：

- `GlobalDispatcher`
  全局域消息分发器。
- `PersistenceRuntime`
  异步持久化任务托管器。
- `Rooms` / `Sessions`
  对外暴露的只读房间与会话索引。
- `Config`
  运行时配置。
- `SyncRoot`
  线程同步锁对象。
- `CurrentRealtimeSinceStartup` / `CurrentUtcNow`
  当前服务端逻辑时钟。
- `RoomScheduler`
  房间分片调度器。
- `RoomRecordingService`
  回放录制服务，可由扩展注入。
- `RoomMembershipNotifier`
  强制移出成员通知器，可由扩展注入。
- `_sessions`
  `SessionId -> Session` 主索引。
- `_connectionToSession`
  `ConnectionId -> Session` 物理连接索引。
- `_accountToSession`
  `AccountId -> Session` 账号索引。
- `_rooms`
  `RoomId -> Room`。
- `_globalModules`
  已注册的服务端全局模块实例。
- `_unauthenticatedGlobalMsgIds`
  登录前允许访问的全局协议白名单。

关键函数：

- `UpdateRuntimeContext`
  刷新当前统一时间上下文。
- `AttachRoomScheduler`
  绑定房间调度器。
- `FlushOutboundPackets`
  将 `ServerOutboundDispatcher` 中的出站消息真正推到 transport。
- `CaptureRoomRuntimeSnapshots`
  给大厅、监控和 GC 用的轻量快照接口。
- `CaptureRoomDetailedSnapshot`
  取单房间详情快照。
- `CaptureRoomWorkerStats`
  获取 worker 负载数据。
- `Dispose`
  深度销毁全部房间、会话与模块。
- `Tick`
  执行房间与离线会话 GC。
- `OnReceivePacket`
  服务端收包总入口：匿名会话分配、序号防重放、鉴权校验、按作用域路由。
- `SendMessageToSession<T>`
  服务端向指定会话发送 S2C 协议。
- `CreateRoom/GetRoom/DestroyRoom/RegisterRoomToScheduler`
  房间创建、查询、销毁、接入调度器。
- `RegisterUnauthenticatedGlobalProtocol`
  注册登录前可访问的全局 C2S 协议。
- `RegisterGlobalModule/GetGlobalModule/TryGetGlobalModule`
  管理服务端全局模块。
- `GetSessionByAccountId/RegisterSession/RemoveSession`
  管理会话主索引与派生索引。
- `BindConnection/UnbindConnection`
  处理同一业务会话与物理连接的绑定变化。
- `TryGetSessionByConnectionId`
  底层连接到会话的内部查询。

### 4.2 `Runtime/Server/Core/Session.cs`

文件职责：

- 服务端会话实体。
- 保存连接状态、房间状态、授权状态、序号基线、活跃时间。

关键字段/属性：

- `_connectionId`
  当前物理连接 ID。
- `_currentRoomId`
  当前正式所在房间。
- `_authorizedRoomId`
  当前被授权允许进入的房间。
- `_recoverableRoomId`
  当前唯一可恢复房间。
- `_isOnline`
  物理连接是否在线。
- `_isRoomReady`
  当前成员是否已完成房间装配。
- `_lastOfflineTime`
  最近一次离线时间。
- `_lastReceivedSeq`
  最近一次接收并通过校验的序号。
- `_lastActiveRealtime`
  最近任意业务包活跃时间。
- `_lastRoomActiveRealtime`
  最近房间包活跃时间。
- `SessionId` / `AccountId`
  会话和账号标识。
- `IsAuthenticated`
  是否已经不是 `UNAUTH` 会话。

关键函数：

- `UpdateConnection`
  重绑物理连接并刷新在线态。
- `MarkOffline`
  标记离线并清空房间 ready 状态。
- `MarkActive` / `MarkRoomActive`
  刷新活跃时间。
- `BindRoom` / `UnbindRoom`
  正式绑定或解绑房间。
- `AuthorizeRoom` / `ClearAuthorizedRoom`
  设置或清理进房授权。
- `SetRecoverableRoom` / `ClearRecoverableRoom`
  设置或清理可恢复房间。
- `SetRoomReady`
  更新房间装配完成状态。
- `TryConsumeSeq`
  递增消费消息序号，用于防重放/乱序保护。
- `ResetSeq`
  重置序号基线，常见于顶号或重绑连接。

### 4.3 `Runtime/Server/Core/RoomInstance/Room.cs`

文件职责：

- 服务端房间实体。
- 负责成员管理、组件生命周期、房间广播、录制、重连快照、周期 Tick。

类型：

- `RoomState`
  `Waiting` / `Playing` / `Finished`。
- `Room`

关键状态：

- `RoomId`
  房间 ID。
- `Config`
  `RoomConfigModel`，保存静态房间配置。
- `Dispatcher`
  房间域协议分发器。
- `IsRecording`
  当前是否正在录制。
- `CurrentTick`
  房间 Tick 计数器。
- `CreateTime`
  房间创建时间。
- `EmptySince`
  房间变为空房的时间。
- `ComponentIds`
  当前挂载的组件清单。
- `State`
  当前房间状态。
- `LastReplayId`
  最近生成的录像 ID。
- `_members`
  当前成员表。
- `_suspendedMemberIds`
  处于挂起可恢复状态的成员集合。
- `_components`
  挂载的房间组件。
- `_tickableComponents`
  需要 `OnTick` 的组件子集。
- `_outboundDispatcher`
  统一出站队列。
- `_roomRecordingService`
  回放录制服务。
- `_roomMembershipNotifier`
  强制踢出时的扩展通知器。
- `_assignedWorkerId` / `_workerAverageTickMs`
  当前被哪个 worker 执行及其负载指标。
- `_runtimeSnapshot`
  当前轻量快照缓存。

关键函数：

- `UpdateRuntimeContext`
  刷新当前房间的统一时间上下文。
- `SetAssignedWorker` / `UpdateWorkerMetrics`
  写入 worker 分配与耗时指标。
- `GetRuntimeSnapshot`
  返回轻量快照缓存。
- `CaptureDetailedSnapshot`
  生成完整详情快照。
- `SetComponentIds`
  写入房间组件清单。
- `AddComponent/GetComponent/InitializeComponents`
  房间组件装配链。
- `AddMember/TryAddMember`
  尝试正式加入成员。
- `NotifyMemberJoined/RemoveMember/GetMember`
  成员加入、离开、查询。
- `NotifyMemberOffline/NotifyMemberOnline`
  在线状态变化通知组件。
- `SuspendMember/ResumeMember`
  挂起与恢复成员。
- `TriggerReconnectSnapshot`
  对某个恢复成员补发房间快照。
- `StartGame/EndGame`
  切换对局状态并驱动录制链。
- `BroadcastMessage<T>`
  房间内向所有 ready 且在线成员广播。
- `SendMessageTo<T>`
  房间内定向发送。
- `RecordMessageToReplay<T>`
  只写回放、不发网络。
- `StartRecord/StopRecordAndSave`
  控制录像。
- `DispatchPacket`
  房间包分发入口。
- `Tick`
  推进房间 Tick、驱动可 Tick 组件、周期录制、结算超时清理。
- `Destroy`
  销毁房间及其组件、解绑成员、清空分发器。
- `RefreshRuntimeSnapshotUnsafe/BuildRuntimeSnapshotUnsafe`
  维护快照缓存。

### 4.4 `Runtime/Server/Core/RoomInstance/RoomConfigModel.cs`

文件职责：

- 房间静态配置模型，供所有房间组件共享读取。

字段/属性：

- `RoomName`
  房间展示名。
- `MaxMembers`
  最大人数。
- `Password`
  房间密码；为空即公开房。
- `EnableReplayRecording`
  是否允许录制回放。
- `IsPrivate`
  由 `Password` 推导出的私有房判断。
- `CustomProperties`
  承接建房请求透传的自定义业务属性。

### 4.5 `Runtime/Server/Core/RoomInstance/RoomSnapshots.cs`

文件职责：

- 定义给大厅、监控、GC、无头控制台使用的房间快照结构。

类型与字段：

- `RoomRuntimeSnapshot`
  - `RoomId` / `RoomName`
  - `State`
  - `MemberCount` / `OnlineMemberCount`
  - `MaxMembers`
  - `IsPrivate`
  - `CurrentTick`
  - `IsRecording`
  - `LastReplayId`
  - `ComponentIds`
  - `CreateTimeUtc`
  - `EmptySinceUtc`
  - `AssignedWorkerId`
  - `WorkerAverageTickMs`
- `RoomMemberSnapshot`
  - `SessionId`
  - `AccountId`
  - `ConnectionId`
  - `IsOnline`
  - `IsRoomReady`
  - `LastActiveRealtime`
  - `LastRoomActiveRealtime`
- `RoomDetailedSnapshot`
  - `Runtime`
  - `CustomPropertyCount`
  - `CustomProperties`
  - `Members`

### 4.6 `Runtime/Server/Core/ServerDispatchers.cs`

文件职责：

- 存放服务端全局域与房间域消息处理器表。

类型与成员：

- `GlobalDispatcher`
  - `_handlers`
    `MsgId -> Action<Session, Packet>`。
  - `Register`
    注册处理器。
  - `Dispatch`
    按 `MsgId` 调用处理器。
  - `Clear`
    清空处理器表。
- `RoomDispatcher`
  与 `GlobalDispatcher` 相同，但绑定具体 `_roomId` 用于日志与房间语义。

### 4.7 `Runtime/Server/Core/ServerRoomComponent.cs`

文件职责：

- 服务端房间组件基类。

成员：

- `Room`
  所属房间，由工厂注入。
- 业务组件通常重写：
  `OnInit`、`OnDestroy`、`OnMemberJoined`、`OnMemberLeft`、`OnMemberOffline`、`OnMemberOnline`、`OnSendSnapshot`、`OnGameStart`、`OnGameEnd`。

### 4.8 `Runtime/Server/Core/ServerRoomFactory.cs`

文件职责：

- 按组件 ID 装配服务端房间组件。

成员：

- `ComponentBinder`
  自动生成消息绑定器。
- `Registry`
  服务端组件构造器注册表。
- `Register`
  注册组件构造器。
- `Clear`
  清空注册表。
- `BuildComponents`
  批量创建组件、挂到房间、绑定处理器、最后统一初始化。

### 4.9 `Runtime/Server/Core/IRoomMembershipNotifier.cs`

文件职责：

- 由 Runtime 声明“强制移出成员时需要通知对方”，具体通知协议交给扩展层实现。

成员：

- `NotifyForcedLeave(Room room, Session session)`

### 4.10 `Runtime/Server/Core/IRoomRecordingService.cs`

文件职责：

- 抽象房间录像服务，让 `Room` 不依赖具体回放模块。

典型职责：

- 是否允许开始录制
- 开始/停止/中止录制
- 记录初始快照、周期快照、普通消息、结束快照

### 4.11 `Runtime/Server/Core/ITickableComponent.cs`

文件职责：

- 标记某个房间组件需要随房间 Tick 调用 `OnTick`。

成员：

- `OnTick()`

---

## 5. Server/Infrastructure

### 5.1 `Runtime/Server/Infrastructure/ServerRuntimeHost.cs`

文件职责：

- 服务端独立运行宿主。
- 在非 Unity `Update` 中驱动 transport 网络泵、逻辑 Tick、主线程外动作队列。

关键状态：

- `_serverApp`
  当前服务端逻辑宿主。
- `_serverTransportPump`
  如果传输层支持主动泵，则由宿主线程驱动。
- `_roomScheduler`
  房间分片调度器。
- `_pendingActions`
  等待在服务端主线程执行的动作。
- `_workSignal`
  唤醒事件。
- `_stopwatch`
  统一逻辑时钟。
- `_serverThreadId`
  当前宿主线程 ID。

关键函数：

- `Start/Stop`
  启停服务端宿主线程和房间调度器。
- `ExecuteOrEnqueue`
  当前线程若是服务端线程则直接执行，否则排队。
- `RunLoop`
  宿主主循环：更新时间、泵网络、执行排队动作、推送出站、跑 Tick。
- `DrainPendingActions`
  执行动作队列。
- `CalculateWaitMilliseconds`
  计算离下一个 Tick 的等待时间，减少空转。

### 5.2 `Runtime/Server/Infrastructure/ServerRoomScheduler.cs`

文件职责：

- 把房间分配到多个 worker 线程。
- 把房间域消息转发到对应 worker 串行执行。

内部类型：

- `RoomWorker`
  单个房间工作线程，拥有：
  - `_rooms`
    当前 worker 托管的房间。
  - `_pendingActions`
    当前 worker 动作队列。
  - `_averageTickMs`
    平滑平均 Tick 耗时。
  - `LoadScore`
    用于负载均衡选 worker 的综合指标。

外层关键成员：

- `_workers`
  全部房间 worker。
- `_roomToWorker`
  `RoomId -> WorkerIndex` 映射。

关键函数：

- `Start/Stop/Dispose`
  启停全部 worker。
- `RegisterRoom`
  为房间挑一个负载较低的 worker 并登记。
- `UnregisterRoom`
  解除房间与 worker 的绑定。
- `DispatchPacketToRoom`
  深拷贝房间包载荷后投递到目标 worker。
- `GetAssignedWorkerId`
  查询房间被分配到哪个 worker。
- `CaptureWorkerStats`
  取所有 worker 的负载快照。
- `SelectWorkerIndex`
  按 `LoadScore` 选最优 worker。
- `ResolveWorkerCount`
  按配置或 CPU 数量计算 worker 数。

### 5.3 `Runtime/Server/Infrastructure/ServerOutboundDispatcher.cs`

文件职责：

- 统一托管服务端出站发包。
- 房间 worker 线程只负责入队，不直接碰 transport。

内部类型：

- `OutboundEnvelope`
  单播或群发封套，包含：
  - `ConnectionId`
  - `ConnectionIds`
  - `ConnectionCount`
  - `Packet`
  - `PayloadFromPool`

关键成员/函数：

- `_queue`
  出站队列。
- `PendingCount`
  当前待发送数量。
- `EnqueueSingle`
  入队单播。
- `EnqueueMany`
  入队群发。
- `Drain`
  在服务端主宿主线程内真正调用 `transport.SendToClient` 并归还池化载荷。

### 5.4 `Runtime/Server/Infrastructure/ServerPersistenceRuntime.cs`

文件职责：

- 管理异步持久化任务的生命周期，不关心任务具体业务内容。
- 解决“停机时哪些任务要等、哪些任务可以丢”的问题。

类型与成员：

- `PersistencePendingInfo`
  - `Id`
  - `Owner`
  - `Operation`
  - `StartUtc`
  - `JoinOnShutdown`
  - `Elapsed`
- `PersistenceDrainResult`
  - `Completed`
  - `Pending`
  - `PendingCount`
- `ServerPersistenceRuntime`
  - `_pending`
    当前所有跟踪任务。
  - `_joinOnShutdownIdleSource`
    用于等待“必须排空”的任务集归零。
  - `_joinOnShutdownPendingCount`
    需要停机等待的任务数。
  - `_isShuttingDown`
    是否已进入停机模式。

关键函数：

- `TrackAsync(...)`
  跟踪一个异步任务，可区分是否需要停机等待。
- `BeginShutdown()`
  标记进入停机模式，默认阻止新任务启动。
- `SnapshotPending()`
  获取当前挂起任务快照。
- `WaitForIdleAsync`
  等待所有 `joinOnShutdown` 任务完成。
- `Register/Complete`
  内部登记与清理任务生命周期。

### 5.5 `Runtime/Server/Infrastructure/HeadlessServerLogger.cs`

文件职责：

- 无头模式服务端控制台与错误日志保留组件。
- 负责：
  - 异步写磁盘日志
  - 控制台命令解析
  - 房间/会话/日志搜索
  - 平滑停机

核心状态：

- `_logQueue`
  待落盘日志队列。
- `_recentLogQueue`
  最近错误日志缓存。
- `_appManager`
  当前 `StellarNetAppManager`。
- `_roomComponentMetaMap`
  组件元数据缓存。

主要功能组：

- 日志线程：
  `InitializeLogFile`、`HandleLogMessage`、`LogWriteLoop`、`CleanupExpiredLogs`
- 控制台读命令：
  `ConsoleReadLoop`、`ExecuteCommand`
- 关服流程：
  `ShutdownServerRoutine`
- 状态查询：
  `PrintStatusTable`、`PrintRoomsTable`、`PrintSessionsTable`、`PrintRoomDetail`、`PrintSessionDetail`
- 日志检索：
  `PrintRecentLogs`、`SearchLogs`、`PrintLogFiles`

这份脚本体量大，但本质上是“无头运维工具”，不是房间主逻辑。

---

## 6. Shared/Core

### 6.1 `Runtime/Shared/Core/NetCoreDefines.cs`

文件职责：

- 定义协议特性、组件特性、模块特性和运行时元数据结构。

类型与成员：

- `NetScope`
  - `Global`
  - `Room`
- `NetDir`
  - `C2S`
  - `S2C`
- `NetMsgAttribute`
  - `Id`
  - `Scope`
  - `Dir`
  - `AllowUnauthenticated`
- `NetHandlerAttribute`
  标记处理函数。
- `RoomComponentAttribute`
  - `Id`
  - `Name`
  - `DisplayName`
- `ServerModuleAttribute`
  - `Name`
  - `DisplayName`
- `ClientModuleAttribute`
  - `Name`
  - `DisplayName`
- `NetMessageMeta`
  运行时协议元信息：`Id`、`Scope`、`Dir`。
- `RoomComponentMeta`
  组件展示元信息：`Id`、`Name`、`DisplayName`。
- `GlobalModuleMeta`
  模块展示元信息：`Name`、`DisplayName`。

### 6.2 `Runtime/Shared/Core/NetTransportDefines.cs`

文件职责：

- 定义传输层统一包结构 `Packet`。

字段：

- `Seq`
  会话内自增包序号。
- `MsgId`
  协议 ID。
- `Scope`
  作用域。
- `RoomId`
  房间消息上下文。
- `Payload`
  原始载荷数组。
- `PayloadOffset`
  载荷偏移。
- `PayloadLength`
  有效载荷长度。

构造函数：

- 完整构造
  支持带偏移的 `ArraySegment` 风格载荷。
- 简化构造
  默认偏移为 0。

### 6.3 `Runtime/Shared/Core/INetworkTransport.cs`

文件职责：

- 抽象所有传输层共同事件和行为。

成员：

- 服务端事件：
  `OnServerStartedEvent`、`OnServerStoppedEvent`、`OnServerClientConnectedEvent`、`OnServerClientDisconnectedEvent`、`OnServerReceivePacketEvent`
- 客户端事件：
  `OnClientStartedEvent`、`OnClientStoppedEvent`、`OnClientConnectedEvent`、`OnClientDisconnectedEvent`、`OnClientReceivePacketEvent`
- 发包/断连：
  `SendToServer`、`SendToClient`、`DisconnectClient`
- 生命周期：
  `StartServer`、`StartClient`、`StartHost`、`StopServer`、`StopClient`
- 其他：
  `GetRTT`、`ApplyConfig`

### 6.4 `Runtime/Shared/Core/IServerTransportPump.cs`

文件职责：

- 标记某个传输层支持被服务端宿主主动驱动网络泵。

成员：

- `PumpServer()`

### 6.5 `Runtime/Shared/Core/NetMessageMapper.cs`

文件职责：

- 运行时协议索引中心。
- 只读生成表，不在运行时反射扫描业务协议。

关键状态：

- `TypeToMetaCache`
  `Type -> NetMessageMeta`。
- `MsgIdToTypeCache`
  `MsgId -> Type`。
- `_isInitialized`
  是否已成功导入生成表。

关键函数：

- `Initialize()`
  导入 `AutoMessageMetaRegistry`，校验双向映射一致性。
- `GenerateIntegrityReport()`
  打印协议数量统计。
- `TryGetMeta(Type msgType, out NetMessageMeta meta)`
  发送链使用：`Type -> Meta`。
- `TryGetMessageType(int msgId, out Type msgType)`
  收包链使用：`MsgId -> Type`。

---

## 7. Shared/Infrastructure

### 7.1 `Runtime/Shared/Infrastructure/NetInfrastructure.cs`

文件职责：

- 定义序列化抽象与默认实现。

类型与成员：

- `INetSerializer`
  - `Serialize(object obj)`
  - `Serialize(object obj, byte[] buffer)`
  - `Deserialize(byte[] data, int offset, int length, Type type)`
- `ILiteNetSerializable`
  高频消息自定义二进制接口：
  - `Serialize(BinaryWriter writer)`
  - `Deserialize(BinaryReader reader)`
- `LiteNetSerializer`
  默认混合序列化器：
  - 对实现 `ILiteNetSerializable` 的类型走手写二进制
  - 其他类型走 JSON
  - 支持写入外部 buffer 以减少发包时分配

### 7.2 `Runtime/Shared/Infrastructure/NetConfig.cs`

文件职责：

- 定义运行时配置结构和配置加载器。

类型与字段：

- `ConfigRootPath`
  - `StreamingAssets`
  - `PersistentDataPath`
- `NetConfig`
  - `Ip`
  - `Port`
  - `MaxConnections`
  - `TickRate`
  - `MaxRoomLifetimeHours`
  - `OfflineTimeoutLobbyMinutes`
  - `OfflineTimeoutRoomMinutes`
  - `EmptyRoomTimeoutMinutes`
  - `MinClientVersion`
  - `RoomWorkerCount`
  - `RoomWorkerReserveCpuCount`
- `NetConfigLoader`
  - 常量：`ConfigFolderName`、`ConfigFileName`、`RuntimeRootFileName`
  - `LoadRuntimeConfigAsync/Sync`
  - `LoadRuntimeRootAsync/Sync`
  - `LoadAsync`
  - `LoadServerConfigSync`
  - `SaveRuntimeRootSelection`（仅编辑器）
  - `LoadEditorSync`（仅编辑器）
  - 内部 `NormalizeConfig`
    对非法值回退安全默认值

### 7.3 `Runtime/Shared/Infrastructure/NetLogger.cs`

文件职责：

- 提供统一日志格式。

成员：

- `LogInfo`
  条件编译的普通日志。
- `LogWarning`
  条件编译的告警日志。
- `LogError`
  永远输出的错误日志。
- `FormatMessage`
  拼装统一格式的富文本日志。

### 7.4 `Runtime/Shared/Infrastructure/UnityPlayerLoopDispatcher.cs`

文件职责：

- 把“后台线程投递到主线程”和“每轮 PlayerLoop 周期回调”抽象成统一设施。

关键状态：

- `PendingActions`
  待主线程执行动作队列。
- `RecurringCallbacks`
  周期回调表。
- `_mainThreadId`
  Unity 主线程 ID。
- `_isInstalled`
  是否已插入 PlayerLoop。

关键函数：

- `EnsureInstalled`
  把调度系统插入 `EarlyUpdate`。
- `ExecuteOrPost`
  主线程直接执行，后台线程入队。
- `RegisterRecurring/UnregisterRecurring`
  注册或注销每轮执行的回调。
- `Run`
  PlayerLoop 实际入口。
- `RunRecurringCallbacks`
  执行周期回调。
- `DrainPendingActions`
  执行投递动作。
- `ContainsSystem/TryInsertIntoPhase/AppendToRoot`
  PlayerLoop 插入辅助函数。

---

## 8. Transports

### 8.1 `Runtime/Transports/Common/LitePacketFormatter.cs`

文件职责：

- `Packet` 的轻量二进制编解码器。

成员：

- `GetSerializedLength(Packet packet)`
  计算序列化后长度。
- `Serialize(Packet packet, byte[] buffer, int startOffset = 0)`
  把 `Packet` 写进 buffer。
- `TryDeserialize(byte[] data, int startOffset, int length, out Packet packet)`
  从字节片段恢复 `Packet`。

注意点：

- `RoomId` 采用单字节长度前缀，因此 UTF8 编码后不能超过 255 字节。
- `PayloadOffset` + `PayloadLength` 设计是为了减少不必要拷贝。

### 8.2 `Runtime/Transports/TCP/TcpTransportProvider.cs`

文件职责：

- 基于 `TcpClient/TcpListener` 的 TCP 传输实现。
- 生产环境重点链路之一。

关键设计点：

- 每个 TCP 连接都有独立的 4 字节长度前缀帧。
- `ReadExactAsync` 负责把“流式分片读取”拼成完整帧，因此不会把半包误交给上层。
- `TcpSendQueue` + `PendingSendFrame` 让每条连接同一时刻只有一个发送循环，避免高负载下每个包都起一个等待写流任务。
- 发送前先把 `Packet` 序列化成独立帧，避免上层提前归还池化 buffer 后后台任务读到脏数据。

内部类型：

- `TcpConnection`
  服务器端单连接包装：`Id`、`Client`、`SendQueue`。
- `TcpSendQueue`
  单连接发送上下文：`Stream`、`PendingFrames`、`SendLoopRunning`。
- `PendingSendFrame`
  已序列化完毕的一帧：`Buffer`、`Length`。

关键状态：

- `_serverListener`
  TCP 服务端监听器。
- `_serverConnections`
  当前所有服务端连接。
- `_client` / `_clientStream`
  当前 TCP 客户端连接与流。
- `_clientSendQueue`
  客户端发送队列。
- `_isPhysicalConnected`
  真实物理连接状态。

关键函数：

- `StartServer/StopServer`
  启停服务端监听。
- `AcceptClientsAsync`
  接受新连接并起收包循环。
- `ReceiveDataAsync`
  服务端单连接收包循环，按帧读满后解包。
- `StartClient/StopClient`
  启停客户端连接。
- `HandlePhysicalDisconnect`
  统一处理物理断线清理。
- `ReceiveClientDataAsync`
  客户端收包循环。
- `QueueSerializedSend`
  先序列化完整帧，再入单连接发送队列。
- `SendQueuedFramesAsync`
  串行写流。
- `DrainPendingFrames`
  回收待发送帧 buffer。
- `ReadExactAsync`
  从 TCP 流中读取固定长度字节。

### 8.3 `Runtime/Transports/KCP/KcpTransportProvider.cs`

文件职责：

- 基于 `kcp2k` 的可靠 UDP 传输实现。

关键特点：

- 同时实现 `INetworkTransport` 与 `IServerTransportPump`。
- 服务端由 `ServerRuntimeHost` 主动调用 `PumpServer()` 驱动 KCP。
- 客户端通过 `UnityPlayerLoopDispatcher` 注册周期泵。
- KCP 天生不是字节流协议，不存在 TCP 那种“读半帧”的流式粘拆包问题，但仍要依赖 `LitePacketFormatter` 保证上层 `Packet` 结构一致。

关键状态：

- `KcpConfig`
  当前 KCP 参数预设。
- `_server` / `_client`
  kcp2k 服务器和客户端。
- `_isServerActive` / `_isClientActive`
  逻辑活跃状态。
- `_isPhysicalConnected`
  真实物理连接状态。
- `_clientPumpRegistrationId`
  客户端周期泵注册 ID。
- `*_TotalPackets` / `*_DeserializeFailures`
  收包与解包统计。

关键函数：

- `PumpServer`
  服务端网络泵，由 `ServerRuntimeHost` 驱动。
- `StartServer/StopServer`
  启停 KCP 服务端。
- `OnServerDataReceived`
  服务端收包并直接向 `ServerApp` 上抛。
- `StartClient/StopClient`
  启停 KCP 客户端。
- `EnsureClientPumpRegistered/UnregisterClientPump`
  管理客户端周期泵。
- `OnClientDataReceived`
  客户端收包回主线程分发。

### 8.4 `Runtime/Transports/UDP/UdpTransportProvider.cs`

文件职责：

- 基于 `UdpClient` 的实验性 UDP 传输实现。

定位说明：

- 文件注释已经明确：只用于教学和实验，不建议承载生产业务主链。
- 没有像 KCP 那样的可靠性、重传、拥塞控制，也没有业务层会话恢复能力。

关键状态与函数：

- `_endpointToConnId` / `_connIdToEndpoint`
  用于把 `IPEndPoint` 映射成伪连接 ID。
- `ReceiveServerDataAsync`
  服务端 UDP 收包循环。
- `ReceiveClientDataAsync`
  客户端 UDP 收包循环。
- `HandlePhysicalDisconnect`
  统一处理客户端断开清理。

---

## 9. Runtime 阅读优先级建议

如果你只想先抓住主链，建议按这个顺序看源码：

1. `StellarNetAppManager.cs`
2. `ClientApp.cs`
3. `ServerApp.cs`
4. `Session.cs`
5. `Room.cs`
6. `ServerRuntimeHost.cs`
7. `ServerRoomScheduler.cs`
8. `NetCoreDefines.cs`
9. `NetMessageMapper.cs`
10. `TcpTransportProvider.cs` / `KcpTransportProvider.cs`

如果你是为了后续二开或排错，这一顺序几乎总是最省时间的。

---

## 10. 高频问题定位表（按现象反查字段与函数）

### 10.1 “消息发了但服务端没处理”

优先检查：

1. `Runtime/Shared/Core/NetMessageMapper.cs`
   - 是否存在该 `MsgId` 的元信息。
2. `Runtime/Server/Core/ServerDispatchers.cs`
   - 是否进入全局/房间分发路径。
3. `Runtime/Server/Core/ServerApp.cs`
   - `OnReceivePacket(...)` 是否被调用。
4. `Runtime/StellarNetAppManager.cs`
   - `HandleServerReceivePacket(...)` 是否把包投递给 `ServerRuntimeHost`。

典型根因：

1. 协议未重新生成，`MsgId` 不在注册表。
2. `NetScope` 与处理器所在域不一致（全局/房间混用）。
3. 服务端尚未创建或已停止（`_serverRuntimeRequested` 状态不符合预期）。

### 10.2 “能进房但房间组件为空”

优先检查：

1. `Runtime/Server/Core/ServerRoomFactory.cs`
   - 服务端组件是否注册且可实例化。
2. `Runtime/Client/Core/ClientRoomFactory.cs`
   - 客户端组件是否注册且可实例化。
3. `Runtime/Server/Core/ServerApp.cs`
   - 建房时是否正确执行装配路径。
4. `Runtime/Client/Core/ClientApp.cs`
   - 进房后是否执行 `BuildComponents(...)`。

典型根因：

1. `[RoomComponent]` 两端 `Id` 不一致。
2. 房间配置里的 `ComponentIds` 未包含目标组件。
3. 生成器没跑，`AutoRegistry` 没有该组件注册代码。

### 10.3 “TCP 高负载下怀疑粘包/拆包错误”

优先检查：

1. `Runtime/Transports/TCP/TcpTransportProvider.cs`
   - `ReadExactAsync(...)`：是否严格按长度前缀读满。
   - `ReceiveDataAsync(...)` / `ReceiveClientDataAsync(...)`：收包循环是否连续。
2. `Runtime/Transports/Common/LitePacketFormatter.cs`
   - `TryDeserialize(...)` 是否成功，失败计数是否异常升高。
3. `TcpSendQueue` 相关路径：
   - `QueueSerializedSend(...)`
   - `SendQueuedFramesAsync(...)`
   - `DrainPendingFrames(...)`

为什么这里通常不会出现“流式粘拆包误分发”：

1. 每帧写入都带 4 字节长度前缀。
2. 读取端先读帧长，再 `ReadExactAsync` 读满帧体。
3. 上层收到的永远是完整帧，不会拿到半帧 payload。

### 10.4 “KCP 在线但客户端表现不更新”

优先检查：

1. `Runtime/Transports/KCP/KcpTransportProvider.cs`
   - `PumpServer()` 是否被 `ServerRuntimeHost` 调用。
   - 客户端泵是否注册成功：`EnsureClientPumpRegistered(...)`。
2. `Runtime/Server/Infrastructure/ServerRuntimeHost.cs`
   - 主循环是否在稳定推进 Tick。
3. `Runtime/Client/Core/ClientApp.cs`
   - `OnReceivePacket(...)` 是否真正被触发。

典型根因：

1. 服务端泵未执行，导致 KCP 包未被驱动。
2. 客户端泵未注册，导致收包回调不推进。
3. 业务层写入未产生脏变化（ObjectSync 不发增量）。

---

## 11. 生产环境传输层自检清单（TCP/KCP）

上线前建议至少确认：

1. `TcpTransportProvider`：
   - `ReadExactAsync(...)` 使用固定长度读取，无短读直传路径。
   - `QueueSerializedSend(...)` 先序列化再排队，不共享可变 payload buffer。
2. `KcpTransportProvider`：
   - 服务端 `PumpServer()` 有稳定驱动源（默认 `ServerRuntimeHost`）。
   - 客户端泵在连接建立后已注册，在断开后能反注册。
3. `LitePacketFormatter`：
   - `TryDeserialize` 失败日志可观测，异常帧不会继续进入业务分发。
4. `ServerDispatchers` / `ClientDispatchers`：
   - 未注册消息会明确告警，不会静默吞包。
5. 运行时日志：
   - 连接建立/断开、收包失败、反序列化失败、消息未注册都有可追踪日志。

这份清单主要用于避免“传输正常、业务不动、但无法快速定位”的生产排障盲区。

---

## 12. 逐脚本字段/函数手册（第一批）

这一批先覆盖主链 7 个脚本：`StellarNetAppManager`、`ClientApp`、`ServerApp`、`Session`、`Room`、`TcpTransportProvider`、`KcpTransportProvider`。

### 12.1 `Runtime/StellarNetAppManager.cs`

字段与状态变化点：

1. `Serializer`
   在 `Awake()` 中固定初始化，运行期通常不再替换。
2. `Transport`
   由 `GetComponent<INetworkTransport>()` 注入；为空会直接中止核心初始化。
3. `_isCoreInitialized`
   仅在 `Awake()` 完成完整装配后置 `true`，用于阻断未初始化阶段的误调用。
4. `_serverRuntimeRequested`
   由 `StartServer/StopServer/StartHost` 写入，表示“逻辑上是否应维持服务端”。
5. `_serverLifecycleLock`
   保护 `EnsureServerRuntimeCreated` 与 `DisposeServerRuntime` 的互斥执行，避免并发启停。
6. `_runtimeFeatureBridges`
   来自 `GeneratedRuntimeAccess.CreateRuntimeFeatureBridges()`；所有扩展钩子都通过该数组扇出。

关键函数执行顺序（启动）：

1. `Awake()`
   装配 `UnityPlayerLoopDispatcher` -> 加载配置 -> 初始化 `NetMessageMapper` -> 装配自动绑定器 -> 注册传输事件。
2. `StartServer()` / `StartHost()`
   先置 `_serverRuntimeRequested=true`，再启动物理层。
3. `HandleServerStarted(...)`
   收到传输层回调后调用 `EnsureServerRuntimeCreated()`，真正创建 `ServerApp` 与 `ServerRuntimeHost`。

关键函数执行顺序（停服）：

1. `StopServer()`
   先置 `_serverRuntimeRequested=false`，再停物理层并调用 `DisposeServerRuntime()`。
2. `OnDestroy()`
   二次兜底解绑事件并销毁服务端宿主，避免场景切换遗留回调。

### 12.2 `Runtime/Client/Core/ClientApp.cs`

字段与状态变化点：

1. `State`
   只能通过 `TryChangeState(...)` 改写，非法迁移会被拒绝并告警。
2. `CurrentRoom`
   在 `EnterOnlineRoom/EnterSandboxRoom` 创建，在 `LeaveRoom/Dispose` 销毁。
3. `_isCurrentRoomConfirmed`
   进房后先为 `false`；仅 `ConfirmCurrentRoom(roomId)` 成功时变 `true`。
4. `_sendSeq`
   每次 `SendMessage<T>` 成功发送后递增，保证同会话包序。
5. `_weakNetBypassMsgIds`
   仅允许 C2S 协议注册；弱网阻断时白名单消息仍可发。

收包与发包核心规则：

1. `OnReceivePacket(Packet packet)`
   `Global` 直接进全局分发；`Room` 需同时满足：非挂起态、`CurrentRoom` 存在、`RoomId` 匹配。
2. `SendMessage<T>(T msg)`
   统一做方向校验、状态校验、弱网阻断校验、房间确认校验，失败则拦截并写日志。

### 12.3 `Runtime/Server/Core/ServerApp.cs`

字段与索引职责：

1. `_sessions`
   `SessionId -> Session` 主索引，所有会话回收与遍历都从这里开始。
2. `_connectionToSession`
   `ConnectionId -> Session`，用于底层收包快速路由。
3. `_accountToSession`
   `AccountId -> Session`，用于顶号与重连复用。
4. `_rooms`
   `RoomId -> Room`，房间生命周期管理入口。
5. `_unauthenticatedGlobalMsgIds`
   登录前允许访问的全局协议白名单；未命中会被 `OnReceivePacket` 拦截。

关键函数与边界：

1. `OnReceivePacket(int connectionId, Packet packet)`
   负责匿名会话分配、防重放校验、鉴权前访问控制、全局/房间域路由。
2. `SendMessageToSession<T>(Session session, T msg)`
   统一 S2C 发送入口，校验方向必须为 `S2C`，并处理序列化失败与离线会话。
3. `Tick()`
   只做 GC 与生命周期维护，不直接推进房间玩法 Tick（玩法推进由 `RoomScheduler/Room` 负责）。

### 12.4 `Runtime/Server/Core/Session.cs`

字段与并发模型：

1. `_gate`
   会话内部所有可变状态都在该锁内读写，确保跨线程一致性。
2. `_authorizedRoomId`
   表示“可进入但尚未最终进房确认”的房间，典型用于 `C2S_RoomSetupReady` 前置校验。
3. `_recoverableRoomId`
   表示可恢复房间上下文，重连链只认这个单值。
4. `_lastReceivedSeq`
   `TryConsumeSeq(...)` 成功后更新，用于防重放与乱序保护。

关键函数：

1. `UpdateConnection(...)`
   物理重绑连接时刷新在线态与活跃时间。
2. `MarkOffline(...)`
   标记离线并清除 `IsRoomReady`，防止离线会话继续被当成就绪成员。
3. `BindRoom/UnbindRoom`
   正式进房与离房状态切换，不等于授权状态切换。

### 12.5 `Runtime/Server/Core/RoomInstance/Room.cs`

字段与职责切分：

1. `_members`
   当前正式成员；`MemberCount` 直接读取该容器计数。
2. `_suspendedMemberIds`
   记录挂起可恢复成员，供 `ResumeMember` 与恢复链判断。
3. `_components` / `_tickableComponents`
   前者是全部房间组件，后者是 `ITickableComponent` 子集，避免每 Tick 做类型判断。
4. `_runtimeSnapshot`
   轻量快照缓存，供大厅列表、监控、GC 查询，减少高频临时对象构建。

关键函数与时序：

1. `InitializeComponents()`
   房间组件构建完成后统一执行 `OnInit()`。
2. `TryAddMember(...)`
   校验房间状态/人数上限后绑定会话与成员，再按参数决定是否触发组件 `OnMemberJoined`。
3. `DispatchPacket(...)`
   房间域包分发入口；与 `ServerApp.OnReceivePacket` 的房间路由形成上下游链。
4. `Tick()`
   推进 `CurrentTick`、调用可 Tick 组件、执行周期录制与房间结束判定。

### 12.6 `Runtime/Transports/TCP/TcpTransportProvider.cs`

连接与队列模型：

1. `TcpConnection`
   服务端单连接对象，持有 `NetworkStream` 对应的 `TcpSendQueue`。
2. `TcpSendQueue`
   单连接串行发送队列；`SendLoopRunning` 确保单时刻仅一个写循环。
3. `PendingSendFrame`
   已序列化好的独立帧，避免发送阶段依赖上游可变缓冲区。

防粘包/拆包关键点：

1. `ReceiveDataAsync/ReceiveClientDataAsync`
   先读 4 字节长度，再 `ReadExactAsync` 读满帧体。
2. `ReadExactAsync`
   固定长度读取，不够就继续读；不会把半帧提交给 `LitePacketFormatter`。
3. `QueueSerializedSend`
   先序列化完整帧再入队，避免高负载下 payload 生命周期错位。

### 12.7 `Runtime/Transports/KCP/KcpTransportProvider.cs`

泵驱动模型：

1. `PumpServer()`
   服务端 KCP 收发推进入口，由 `ServerRuntimeHost` 调度。
2. `EnsureClientPumpRegistered()`
   客户端在 `UnityPlayerLoopDispatcher` 注册周期泵，避免依赖 MonoBehaviour `Update`。
3. `UnregisterClientPump()`
   断开或停止时反注册，防止重复泵与悬空回调。

数据安全与回调边界：

1. `OnServerDataReceived(...)`
   服务端路径在泵线程内直接上抛 `OnServerReceivePacketEvent`。
2. `OnClientDataReceived(...)`
   客户端路径先复制安全载荷，再投递到主线程触发 `OnClientReceivePacketEvent`。
3. `SendToServer/SendToClient`
   使用独立 `byte[]` 构造发送帧，避免池化数组过早回收导致发送内容污染。

---

## 13. 逐脚本字段/函数手册（第二批）

### 13.1 `Runtime/Client/Core/ClientSession.cs`

字段语义：

1. `SessionId`
   登录成功后写入；为空表示未登录。
2. `AccountId`
   当前账号标识；用于重连补登与展示层用户态。
3. `CurrentRoomId`
   当前正式房间；离房后清空。
4. `LastBoundRoomId`
   最近一次成功绑定房间；重连提示常用。
5. `IsPhysicalOnline`
   物理连接是否在线，与业务是否登录是两个维度。
6. `IsReconnecting`
   是否处于重连流程。

关键函数：

1. `OnLoginSuccess(...)`
   登录成功入口，刷新 `SessionId/AccountId` 并重置重连标记。
2. `BindRoom(...)` / `UnbindRoom()`
   正式进房与离房状态切换。
3. `ClearRecoveryContext()`
   只清恢复上下文，不清登录态。
4. `Clear()`
   深度清理会话状态，通常用于硬断线或主动退出。

### 13.2 `Runtime/Client/Core/ClientRoom.cs`

字段语义：

1. `RoomId`
   当前客户端房间标识。
2. `Dispatcher`
   房间域协议分发器。
3. `NetEventSystem`
   房间本地事件总线。
4. `_components`
   已挂载的 `ClientRoomComponent` 列表。
5. `_isDestroyed`
   销毁幂等保护。

关键函数：

1. `Create(roomId)`
   统一创建入口；`roomId` 为空会直接失败。
2. `AddComponent(...)`
   挂载组件并回填 `component.Room`。
3. `GetComponent<T>()`
   线性查找组件实例。
4. `InitializeComponents()`
   依次执行 `OnInit()`。
5. `Destroy()`
   依次执行组件 `OnDestroy()`，再清分发器与事件系统。

### 13.3 `Runtime/Server/Core/ServerDispatchers.cs`

结构说明：

1. `GlobalDispatcher`
   维护 `MsgId -> Action<Session, Packet>`，用于全局域协议。
2. `RoomDispatcher`
   维护 `MsgId -> Action<Session, Packet>`，用于房间域协议。

共同边界：

1. `Register(...)`
   拒绝空处理器与重复 `MsgId`。
2. `Dispatch(...)`
   找不到处理器时只告警不抛异常，避免单条未知消息拖垮主循环。
3. `Clear()`
   清空注册表，供房间销毁与运行时回收使用。

### 13.4 `Runtime/Server/Infrastructure/ServerRoomScheduler.cs`

内部 `RoomWorker` 关键字段：

1. `_pendingActions`
   跨线程投递到 worker 的动作队列。
2. `_rooms`
   当前 worker 持有的房间集合。
3. `_averageTickMs`
   房间 Tick 平滑耗时指标。
4. `LoadScore`
   负载评分（综合 Tick 耗时、房间数、待执行动作数）。

调度器关键函数：

1. `RegisterRoom(Room room)`
   选取负载最低 worker，建立 `roomId -> worker` 映射并注册房间。
2. `DispatchPacketToRoom(Room room, Session session, Packet packet)`
   深拷贝 payload 后投递到目标 worker，避免跨线程共享缓冲区。
3. `CaptureWorkerStats()`
   输出 `Rooms/Pending/AvgTickMs/LoadScore` 快照供监控面板展示。
4. `UnregisterRoom(...)` / `Dispose()`
   房间解绑与调度器回收。

### 13.5 `Runtime/Server/Infrastructure/ServerRuntimeHost.cs`

关键字段：

1. `_serverTransportPump`
   可选传输泵接口，当前用于 KCP 服务端主动驱动。
2. `_roomScheduler`
   房间分片调度器实例。
3. `_pendingActions`
   外部线程投递到服务端线程执行的动作。
4. `_serverThreadId`
   当前服务端线程标识，用于 `IsCurrentThread` 判断。

关键函数时序：

1. `Start()`
   启动 `_roomScheduler`，再启动服务端宿主线程。
2. `RunLoop()`
   每轮顺序：更新时间上下文 -> 网络泵 -> 执行动作队列 -> Flush 出站 -> 按 TickRate 推进 `ServerApp.Tick()`。
3. `ExecuteOrEnqueue(Action)`
   当前线程是服务端线程则直接执行，否则入队。
4. `Stop()`
   停止调度器、取消线程、清理计时器与标识。

### 13.6 `Runtime/Shared/Core/NetMessageMapper.cs`

核心缓存：

1. `TypeToMetaCache`
   协议类型到元数据映射（发包链常用）。
2. `MsgIdToTypeCache`
   `MsgId` 到协议类型映射（收包链常用）。
3. `_isInitialized`
   防止重复初始化与未初始化查询。

关键函数：

1. `Initialize()`
   从 `AutoMessageMetaRegistry` 导入并做双向一致性校验；冲突会直接抛异常阻断启动。
2. `TryGetMeta(Type msgType, out NetMessageMeta meta)`
   发送前按类型查协议元信息。
3. `TryGetMessageType(int msgId, out Type msgType)`
   收包后按 `MsgId` 查目标类型。

### 13.7 `Runtime/Shared/Infrastructure/UnityPlayerLoopDispatcher.cs`

关键字段：

1. `PendingActions`
   后台线程投递到主线程的动作队列。
2. `RecurringCallbacks`
   每轮 PlayerLoop 都执行的周期回调集合。
3. `_mainThreadId`
   Unity 主线程标识。
4. `_isInstalled`
   是否已插入 PlayerLoop。

关键函数：

1. `EnsureInstalled()`
   将 `Run` 回调插入 `EarlyUpdate`（失败则追加到根节点）。
2. `ExecuteOrPost(Action)`
   主线程直接执行，非主线程入队。
3. `RegisterRecurring/UnregisterRecurring`
   注册与注销周期泵（KCP 客户端等依赖它）。
4. `Run()`
   每轮先执行周期回调，再清空主线程动作队列。
