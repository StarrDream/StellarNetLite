# 17. StellarNet Lite Editor 与 LoadTest 源码参考

本文档覆盖两块：

- `StellarNetLite/Editor`
- `StreamingAssets/StellarNetLiteLoadTest`

这两块虽然不参与正式运行时主链，但它们决定了：

- 框架声明如何变成静态注册代码
- 配置如何被维护
- 新模块如何快速落地
- 服务端如何在编辑器里观测
- 高负载下如何做压测与复现

---

## 1. Editor 总览

`Editor` 层可以按职责拆成四类：

- 生成器
  `LiteProtocolScanner`、`NetPrefabScanner`
- 配置器
  `NetConfigEditorWindow`
- 脚手架与辅助工具
  `StellarNetScaffoldWindow`、`FolderContentCopyTool`
- 运行期运维工具
  `ServerMonitorWindow`、`LoadTestWindow`

---

## 2. 协议与绑定生成器

### 2.1 `Editor/LiteProtocolScanner.cs`

文件职责：

- 编辑器里最核心的扫描器。
- 把源码中声明的 `[NetMsg]`、`[RoomComponent]`、`[ServerModule]`、`[ClientModule]`、`[NetHandler]` 转成 Runtime 真正依赖的静态注册代码。

它生成的核心文件包括：

- `Assets/StellarNetLiteGenerated/MsgIdConst.cs`
- `Assets/StellarNetLiteGenerated/ComponentIdConst.cs`
- `Assets/StellarNetLiteGenerated/AutoMessageMetaRegistry.cs`
- `Assets/StellarNetLiteGenerated/AutoUnauthenticatedProtocolRegistry.cs`
- `Assets/StellarNetLiteGenerated/AutoRegistry.cs`
- `Assets/StellarNetLiteGenerated/AutoRuntimeFeatureRegistry.cs`

核心常量路径：

- `GeneratedRootPath`
- `ProtocolIdsFolderPath`
- `ProtocolMetaFolderPath`
- `ComponentConstFolderPath`
- `BinderRoomFolderPath`
- `BinderModuleFolderPath`
- `MsgIdAggregateOutputPath`
- `ComponentAggregateOutputPath`
- `MessageMetaAggregateOutputPath`
- `UnauthenticatedProtocolRegistryOutputPath`
- `RegistryAggregateOutputPath`
- `RuntimeFeatureRegistryOutputPath`

内部类型：

- `ScanResultState`
  - `NoChange`
  - `Changed`
  - `Failed`
- `MethodMeta`
  单个处理函数扫描结果：
  - `MethodName`
  - `MsgFullName`
  - `MsgId`
  - `FeatureKey`
- `ClassMeta`
  单个模块/组件扫描结果：
  - `Id`
  - `Name`
  - `SafeVarName`
  - `FullName`
  - `DisplayName`
  - `FeatureKey`
  - `Methods`
- `ProtocolMeta`
  单个协议扫描结果：
  - `Id`
  - `SafeName`
  - `FullTypeName`
  - `Scope`
  - `Dir`
  - `AllowUnauthenticated`
  - `FeatureKey`
- `GeneratedFileRecord`
  记录待写入文件：
  - `Path`
  - `Content`

关键入口：

- 静态构造函数
  编辑器加载时自动触发一次扫描。
- `ManualRun()`
  手动重新生成。
- `ForceRun()`
  菜单强制重新生成。
- `RunScanAndGenerate(bool forceWrite)`
  总调度入口：
  - 生成协议常量与元数据
  - 生成组件/模块绑定器
  - 生成运行时扩展桥注册表

这份脚本的意义：

- 没有它，`Runtime` 根本不知道有哪些协议、模块、组件和处理器。
- 这是整个框架从“反射声明”转向“静态注册”的关键。

### 2.2 `Editor/NetPrefabScanner.cs`

文件职责：

- 扫描挂有 `NetIdentity` 的 Prefab。
- 生成稳定的 `PrefabHash` 常量表，供 `ObjectSync` 使用。

关键常量：

- `OutputConstFilePath`
  输出到 `Assets/StellarNetLiteGenerated/NetPrefabConsts.cs`。

关键函数：

- `ManualRun`
  菜单触发手动扫描。
- `RunScanAndGenerate`
  遍历项目 Prefab，筛选符合约定的网络预制体。
- `GetResourcesRelativePath`
  解析 `Resources` 相对路径。
- `GetHashSourceKey`
  生成 hash 输入源，保证稳定性。
- `GetStableHash`
  计算稳定 hash。
- `GenerateValidVariableName`
  把路径/名称转成可用常量名。
- `GenerateConstFile`
  写出常量表。

扫描时重点检查两类冲突：

- `Hash` 冲突
- 变量名冲突

---

## 3. 配置编辑器

### 3.1 `Editor/NetConfigEditorWindow.cs`

文件职责：

- 统一编辑三套配置：
  - `NetConfig`
  - `ReplayGlobalConfig`
  - `ObjectSyncGlobalConfig`
- 但保存时分别落到独立文件。

核心字段：

- `_runtimeConfig`
  运行时网络配置。
- `_replayConfig`
  回放配置。
- `_objectSyncConfig`
  对象同步配置。
- `_targetRoot`
  当前编辑的是 `StreamingAssets` 还是 `PersistentDataPath` 下的配置。
- `_loadedSnapshotJson`
  加载时的快照，用于判断是否脏。
- `_runtimePath` / `_replayPath` / `_objectSyncPath`
  当前解析出来的三个目标文件路径。
- `_scrollPos`
  滚动位置。
- `_headerStyle` / `_pathStyle`
  UI 样式缓存。
- `_stylesInitialized`
  样式初始化标记。

内部枚举：

- `SaveResultState`
  - `NoChange`
  - `Saved`
  - `Failed`

关键函数：

- `ShowWindow`
  打开配置窗口。
- `OnEnable`
  根据当前根目录加载配置。
- `OnGUI`
  绘制整个配置窗口。
- `InitializeStyles`
  初始化样式。
- `DrawToolbar`
  切换运行时根目录、确认脏数据切换。
- `DrawResolvedPathInfo`
  显示当前文件路径。
- `DrawRuntimeFields`
  绘制 `NetConfig` 字段。
- `DrawReplayFields`
  绘制回放配置字段。
- `DrawObjectSyncFields`
  绘制对象同步配置字段。
- `DrawActionButtons`
  保存、重载、切换根目录等操作。
- `LoadFromCurrentRoot`
  从当前根目录同步加载三套配置。

这个窗口的重要性：

- 它把原本散落的多份 JSON 配置统一到一个入口。
- 也是当前框架“配置分拆保存”之后的主要维护面板。

---

## 4. 脚手架与辅助工具

### 4.1 `Editor/StellarNetScaffoldWindow.cs`

文件职责：

- 业务脚手架生成器。
- 用模板快速生成：
  - 房间组件
  - 全局模块

内部枚举：

- `ScaffoldBusinessType`
  - `RoomComponent`
  - `GlobalModule`

核心常量：

- `DefaultOutputRoot`
- `DefaultBaseNamespace`
- `TemplatesPath`

核心字段：

- `_businessType`
  当前生成的是组件还是模块。
- `_namespacePrefix`
  前置命名空间。
- `_outputRoot`
  输出根目录。
- `_featureName`
  英文功能名。
- `_displayName`
  中文显示名。
- `_componentId`
  房间组件 ID。
- `_c2sMsgId`
  生成的 C2S 协议 ID。
- `_s2cMsgId`
  生成的 S2C 协议 ID。

关键函数：

- `Open`
  打开窗口。
- `OnGUI`
  绘制输入表单。
- `GenerateScaffold`
  真正生成脚手架。
- `SanitizeIdentifier`
  规范化英文标识符。
- `SanitizeNamespacePrefix`
  规范化命名空间。
- 模板替换相关函数
  负责把用户输入填进模板内容。

### 4.2 `Editor/FolderContentCopyTool.cs`

文件职责：

- 文件夹源码复制工具。
- 主要给“整理源码给别人看”或“喂给外部 AI/分析工具”使用。

核心字段：

- `_rootFolder`
  当前根目录。
- `_subFolders`
  根目录下一层子目录列表。
- `_selectedFolders`
  当前选中的子目录。
- 文件类型过滤：
  - `_includeCs`
  - `_includeShader`
  - `_includeTxt`
  - `_includeJson`
  - `_includeAsmdef`
  - `_includeMeta`
- 压缩选项：
  - `_optimizeForAI`
  - `_removeComments`
  - `_removeIndentation`

关键函数：

- `ShowWindow`
  打开工具窗口。
- `OnGUI`
  绘制四块 UI：目录、过滤器、列表、操作按钮。
- `DrawHeader`
  选择根目录。
- `DrawFilters`
  过滤扩展名与压缩策略。
- `DrawFolderList`
  选择子目录。
- `DrawActionButtons`
  复制、全选、反选等动作。
- `RefreshSubFolders`
  重新扫描根目录下一层子目录。
- 内容压缩函数
  负责去注释、去缩进、合并空行、拼接 markdown 输出。

### 4.3 `Editor/ServerMonitorWindow.cs`

文件职责：

- 运行期服务端内存状态监控窗口。

核心字段：

- `_roomScroll`
  房间列表滚动位置。
- `_sessionScroll`
  会话列表滚动位置。
- `_selectedRoomId`
  当前选中房间。

关键函数：

- `ShowWindow`
  打开窗口。
- `OnInspectorUpdate`
  运行态下周期刷新。
- `OnGUI`
  主绘制入口，要求 `Application.isPlaying` 且存在 `ServerApp`。
- `DrawOverview`
  全局概览：在线连接数、房间数、TickRate。
- `DrawRoomList`
  房间列表。
- `DrawSessionList`
  会话列表。
- `DrawRoomDetails`
  当前选中房间详情。

它读取的是 `ServerApp` 暴露的快照接口，而不是直接乱读房间内部状态，这一点和运行时设计是一致的。

---

## 5. 编辑器里的压测工具入口

### 5.1 `Editor/LoadTestWindow.cs`

文件职责：

- Unity Editor 压测窗口。
- 作用是启动和控制独立的 `StellarNetLiteLoadTest` 进程，而不是在 Unity 进程里直接模拟机器人。

内部枚举：

- `LoadTestTransport`
  - `Kcp`
  - `Tcp`

核心常量：

- `EditorPrefsPrefix`
  EditorPrefs 键名前缀。
- `RunningPidPrefsKey`
  当前运行进程 PID 的持久化键。
- 默认参数常量：
  `DefaultHost`、`DefaultPort`、`DefaultRoomCount`、`DefaultClientsPerRoom`、`DefaultConnectRate`、`DefaultDuration`、`DefaultMoveRate`、`DefaultLogInterval`、`DefaultRoomEndMinutes`、`DefaultRoomName`、`DefaultAccountPrefix`、`DefaultClientVersion`

核心字段：

- 当前压测参数：
  - `_transport`
  - `_host`
  - `_port`
  - `_roomCount`
  - `_clientsPerRoom`
  - `_redundantClientsPerRoom`
  - `_connectRate`
  - `_duration`
  - `_moveRate`
  - `_logInterval`
  - `_roomEndMinutes`
  - `_enableReplayRecording`
  - `_roomName`
  - `_accountPrefix`
  - `_clientVersion`
- 运行时命令输入：
  - `_runtimeCommandText`
  - `_runtimeRoomDelta`
  - `_runtimeRoomNumber`
- 输出区：
  - `_scrollPos`
  - `_autoScroll`
  - `_scrollToBottomRequested`
  - `_outputBuilder`
  - `_pendingOutputLines`
- 进程跟踪：
  - `_runningProcess`
  - `_outputStyle`
  - `_stylesInitialized`

关键函数：

- `Open`
  打开窗口。
- `OnEnable/OnDisable/OnDestroy`
  恢复参数、注册编辑器回调、清理进程。
- `LoadPrefs/SavePrefs`
  持久化窗口参数。
- `RestoreTrackedProcess`
  读取 PID 并尝试恢复对旧进程的跟踪。
- `StartLoadTestProcess`
  拼接命令行并启动独立压测程序。
- `StopLoadTestProcess`
  停止压测进程。
- `SendRuntimeCommand`
  发送运行时命令，如 `status`、`addroom`、`endroom`。
- `OnEditorUpdate`
  拉取子进程输出并刷新窗口。

---

## 6. Standalone LoadTest 工具

这部分位于 `StreamingAssets/StellarNetLiteLoadTest`，虽然是独立 .NET 程序，但它是当前框架高负载验证的重要工具。

### 6.1 `StellarNetLiteLoadTest/README.md`

文档职责：

- 说明当前压测工具支持：
  - `kcp` / `tcp`
  - 多房间
  - 每房多客户端
  - 运行时房间控制命令
  - 自动 `Ready` / `StartGame`
  - 行为模拟：移动 / 动作 / 气泡
  - `duration = 0` 无限运行
- 给出构建命令、运行示例和参数表。
- 记录传输层验收口径：KCP/TCP 都按 `5 rooms x 20 clients x 10 min` 做 100 CCU 验证；预期无传输错误、无解包失败、无异常状态超时爆发，TCP 发送队列指标不持续增长。

### 6.2 `StellarNetLiteLoadTest/Program.cs`

文件职责：

- 压测程序主入口。
- 同时包含参数解析器、总调度器、机器人状态机和统计器。

#### `Program`

成员：

- `Main`
  - 解析 `LoadTestOptions`
  - 创建取消令牌
  - 启动命令输入任务
  - 启动 `LoadTestRunner`
- `ReadCommandsAsync`
  从标准输入读取运行时命令。

#### `LoadTestOptions`

字段/属性：

- `Transport`
- `Host`
- `Port`
- `RoomCount`
- `ClientsPerRoom`
- `RedundantClientsPerRoom`
- `ConnectRate`
- `DurationSeconds`
- `MoveRate`
- `RoomName`
- `AccountPrefix`
- `ClientVersion`
- `LogIntervalSeconds`
- `RoomEndMinutes`
- `EnableReplayRecording`
- `VerboseBotLogs`
- `TotalClients`

关键函数：

- `Parse(string[] args)`
  命令行参数解析和合法性校验。
- `PrintUsage()`
  打印用法。
- `RequireValue`
  校验参数值是否存在。

#### `LoadTestRunner`

文件职责：

- 管理所有机器人、房间槽位、运行时命令和统计输出。

核心字段：

- `_pendingCommands` / `_runtimeCommands`
- `_options`
- `_stopwatch`
- `_stats`
- `_bots`
- `_roomIds`
- `_roomActive`
- `_nextConnectAtMs`
- `_lastLogAtMs`

关键函数：

- `AppendRoomBots`
  为一批房间批量创建机器人。
- `EnqueueCommand`
  写入运行时命令队列。
- `RunAsync`
  总循环：
  - 处理命令
  - 按速率启动新机器人
  - 驱动每个机器人 `Tick`
  - 周期性打印统计
- `StartPendingBots`
  控制连入速率。
- `ProcessCommands`
  处理：
  - `addroom`
  - `removeroom`
  - `endroom`
  - `status`
  - `help`
- `AddRooms/RemoveRooms/EndRoom`
  动态房间控制。
- `PrintStats`
  打印聚合指标。

#### `LoadTestBot`

文件职责：

- 单个机器人状态机。

从源码能看出的主要职责：

- 管理自己的 transport 连接
- 发登录、建房/加房、房间最终确认、Ready、StartGame、EndGame、LeaveRoom
- 模拟正常玩家行为：
  - 走路
  - 动作
  - 气泡聊天
- 维护状态超时检测
- 处理控制面 S2C 协议回包

关键行为函数示例：

- `HandlePacket`
- `HandleLoginResult`
- `HandleCreateRoomResult`
- `HandleJoinRoomResult`
- `HandleRoomSetupResult`
- `HandleGameStarted`
- `SendLogin`
- `SendCreateRoom`
- `SendJoinRoom`
- `SendRoomSetupReady`
- `SendReady`
- `SendStartGame`
- `SendMove`
- `SendAction`
- `SendBubble`
- `CheckStateTimeout`

#### `LoadTestStats`

文件职责：

- 聚合统计压测指标。

字段：

- `_startedClients`
- `_connectedClients`
- `_loginSuccess`
- `_roomSetupSuccess`
- `_gameStartedClients`
- `_sentPackets`
- `_receivedPackets`
- `_sentBytes`
- `_receivedBytes`
- `_movePackets`
- `_actionPackets`
- `_bubblePackets`
- `_errors`

函数：

- `MarkStarted/MarkConnected/MarkLoginSuccess/MarkRoomSetupSuccess/MarkGameStarted`
- `MarkSent/MarkReceived`
- `MarkMovePacket/MarkActionPacket/MarkBubblePacket`
- `MarkError`
- `Capture`
  返回 `Snapshot`

### 6.3 `StellarNetLiteLoadTest/Transports.cs`

文件职责：

- 提供压测工具的 KCP/TCP 客户端传输实现。
- 压测工具不把 UDP 纳入正式可靠性验收；UDP 仅用于实验或特殊对照。

类型与成员：

- `ILoadTestTransport`
  - `IsConnected`
  - `Connect`
  - `Stop`
  - `Send`
  - `Pump`
  - `TryDequeue`
- `KcpLoadTestTransport`
  - `_receivedPackets`
  - `_config`
  - `_client`
  - `Connect/Stop/Send/Pump/TryDequeue`
  - `OnDataReceived`
    只保留控制面 S2C 包，不消费高频同步包，以减轻本地压力
- `TcpLoadTestTransport`
  - `_receivedPackets`
  - `_client`
  - `_stream`
  - `_receiveCts`
  - `Connect/Stop/Send/Pump/TryDequeue`
  - `ReceiveLoopAsync`
    用 4 字节长度前缀读完整帧
  - `ReadExactAsync`
    固定长度读取

### 6.4 `StellarNetLiteLoadTest/NetworkPrimitives.cs`

文件职责：

- 给压测工具自带一套“轻量协议与包工具”，避免直接依赖 Unity Runtime 程序集。

类型与字段：

- `NetScope`
  - `Global`
  - `Room`
- `PacketData`
  - `Seq`
  - `MsgId`
  - `Scope`
  - `RoomId`
  - `Payload`
  - `PayloadOffset`
  - `PayloadLength`
- `LitePacketCodec`
  - `Serialize`
  - `TryDeserialize`
- `LiteJson`
  - `Serialize<T>`
  - `Deserialize<T>`
- `MsgIds`
  控制面协议 ID 常量：
  - `C2S_Login`
  - `S2C_LoginResult`
  - `C2S_CreateRoom`
  - `S2C_CreateRoomResult`
  - `C2S_JoinRoom`
  - `S2C_JoinRoomResult`
  - `C2S_LeaveRoom`
  - `S2C_LeaveRoomResult`
  - `C2S_RoomSetupReady`
  - `S2C_RoomSetupResult`
  - `C2S_SetReady`
  - `C2S_StartGame`
  - `S2C_GameStarted`
  - `C2S_EndGame`
  - `S2C_GameEnded`
  - `C2S_SocialMoveReq`
  - `C2S_SocialActionReq`
  - `C2S_SocialBubbleReq`
  - `IsControlPlaneS2C(int msgId)`
- `ComponentIds`
  - `RoomSettings`
  - `SocialRoom`
  - `ObjectSync`

简化 DTO：

- `C2S_Login`
- `S2C_LoginResult`
- `RoomDTO`
- `C2S_CreateRoom`
- `S2C_CreateRoomResult`
- `C2S_JoinRoom`
- `S2C_JoinRoomResult`
- `C2S_RoomSetupReady`
- `C2S_LeaveRoom`
- `S2C_LeaveRoomResult`
- `S2C_RoomSetupResult`
- `C2S_SetReady`
- `C2S_StartGame`
- `S2C_GameStarted`
- `C2S_EndGame`
- `S2C_GameEnded`
- `SocialMovePayload`

序列化器：

- `SocialMoveSerializer`
  将移动数据序列化为固定长度二进制。
- `SocialActionSerializer`
  将动作 ID 序列化为单个 `int`。
- `SocialBubbleSerializer`
  将气泡文本序列化为 UTF8 字节流。

---

## 7. 维护建议

如果你改了协议声明、组件声明、模块声明或运行时扩展桥：

1. 先看 `LiteProtocolScanner.cs`
2. 再重新生成 `StellarNetLiteGenerated`
3. 最后检查 `Runtime` 是否已经通过 `GeneratedRuntimeAccess` / `AutoRegistry` 消费到新内容

如果你要排查“压测和真实客户端表现不一致”：

1. 先看 `NetworkPrimitives.cs` 的协议 ID 与 DTO 是否和正式协议同步
2. 再看 `Transports.cs` 是否只保留了控制面包
3. 再看 `Program.cs` 中机器人是否模拟了与你当前玩法一致的业务顺序

---

## 8. 从声明到运行的生成闭环（新手必做）

这一节解决“我写了协议/组件，为什么运行时没生效”。

### 8.1 协议新增闭环

1. 在 `Shared/Protocol` 新增带 `[NetMsg]` 的协议类型。
2. 在对应模块/组件里新增 `[NetHandler]`。
3. 执行生成器（`LiteProtocolScanner.ManualRun/ForceRun`）。
4. 检查输出：
   - `MsgIdConst.cs`
   - `AutoMessageMetaRegistry.cs`
   - `AutoRegistry.cs`
5. 进运行时验证：
   - 发包后是否命中目标 `NetHandler`。

### 8.2 房间组件新增闭环

1. 服务端与客户端都声明同一 `[RoomComponent(Id,...)]`。
2. 处理器方法加 `[NetHandler]`。
3. 执行生成器。
4. 检查输出：
   - `ComponentIdConst.cs`
   - 组件绑定代码是否包含新组件。
5. 建房时确认 `ComponentIds` 包含该组件。

### 8.3 RuntimeFeature 扩展闭环

1. 在扩展层新增继承 `RuntimeFeatureBridgeBase` 的类型。
2. 执行生成器。
3. 检查 `AutoRuntimeFeatureRegistry.cs` 是否出现该类型。
4. 运行时确认 `OnRuntimeAwake/OnServerAppCreated/OnClientAppCreated` 等钩子被调用。

---

## 9. 生成器与压测工具排坑表

### 9.1 生成器相关

1. 现象：协议能编译但消息不分发。
   排查：`MsgIdConst` 是否包含该协议；`AutoRegistry` 是否生成对应处理器绑定。
2. 现象：房间组件类存在但 `GetComponent<T>()` 取不到。
   排查：两端 `RoomComponent Id` 是否一致；`ComponentIdConst` 是否更新；建房 `ComponentIds` 是否带上该组件。
3. 现象：运行时扩展桥逻辑不触发。
   排查：`AutoRuntimeFeatureRegistry` 是否注册；类型是否可被反射创建。

### 9.2 压测工具相关

1. 现象：压测在线率低但正式客户端正常。
   排查：`ConnectRate` 是否过高导致连接瞬时拥塞；`Duration` 是否太短。
2. 现象：压测显示协议异常但业务链看似正常。
   排查：`NetworkPrimitives.MsgIds` 与正式协议是否同步。
3. 现象：压测里房间行为顺序不对。
   排查：`LoadTestBot` 的 `SendCreateRoom/SendJoinRoom/SendRoomSetupReady/SendReady/SendStartGame` 是否符合你当前服务端流程。
4. 现象：TCP 压测前几分钟正常，随后延迟越来越高。
   排查：服务端日志里的 TCP pending frame/bytes、write failure、queue abort 是否持续增长；如果增长，说明物理链路或消费端已经阻塞，应该先查发送队列和断线重连恢复。
5. 现象：KCP/TCP 100 CCU 验收偶发 RTT 尖峰。
   排查：先看是否只是短暂 50-80ms jitter；只要无解包失败、无 transport error、业务包持续到达且 RTT 自动恢复，一般不视为物理断开。

---

## 10. 逐脚本字段/函数手册（Editor + LoadTest）

### 10.1 `Editor/LiteProtocolScanner.cs`

核心内部结构：

1. `MethodMeta`
   单处理函数扫描结果（方法名、消息类型、`MsgId`、FeatureKey）。
2. `ClassMeta`
   单模块/组件扫描结果（Id、显示名、类型全名、处理函数列表）。
3. `ProtocolMeta`
   协议扫描结果（`MsgId`、Scope、Dir、未鉴权标记）。
4. `GeneratedFileRecord`
   待写入文件记录（路径+内容）。

关键入口函数：

1. `ManualRun()`
   常规手动生成。
2. `ForceRun()`
   强制重生成（菜单：`StellarNetLite/强制重新生成协议与组件常量表`）。
3. `RunScanAndGenerate(bool forceWrite)`
   总调度入口：扫描 -> 生成 -> 写盘 -> 刷新。

### 10.2 `Editor/NetPrefabScanner.cs`

关键函数：

1. `ManualRun()`
   菜单触发生成 `NetPrefabConsts`。
2. `RunScanAndGenerate()`
   扫描网络预制体并构建常量表内容。

关键输出：

1. `Assets/StellarNetLiteGenerated/NetPrefabConsts.cs`
   ObjectSync 生成实体时依赖的 `PrefabHash` 常量来源。

### 10.3 `Editor/NetConfigEditorWindow.cs`

关键字段：

1. `_runtimeConfig` / `_replayConfig` / `_objectSyncConfig`
   三套配置对象。
2. `_runtimePath` / `_replayPath` / `_objectSyncPath`
   当前配置文件解析路径。
3. `_loadedSnapshotJson`
   用于脏数据比较，防止误覆盖。

关键函数：

1. `ShowWindow()` / `OnEnable()` / `OnGUI()`
   窗口生命周期。
2. `DrawRuntimeFields()` / `DrawReplayFields()` / `DrawObjectSyncFields()`
   三套配置绘制入口。
3. `LoadFromCurrentRoot()` / `SaveToCurrentRoot()`
   统一加载与保存。
4. `ApplyRuntimeRootSelection()` / `RefreshPaths()`
   切换配置根目录并刷新路径。

### 10.4 `Editor/LoadTestWindow.cs`

关键字段：

1. 压测参数域（`_host/_port/_roomCount/_clientsPerRoom/...`）
2. 输出域（`_outputBuilder/_pendingOutputLines/_scrollPos`）
3. 进程域（`_runningProcess` + 持久化 PID）

关键函数：

1. `StartLoadTest()`
   组装命令行并拉起独立压测进程。
2. `TryStopProcess()`
   停止运行中的压测进程。
3. `SendRuntimeCommand(...)`
   向压测进程发送运行时命令。
4. `OnEditorUpdate()`
   拉取进程输出并刷新显示。
5. `RestoreTrackedProcess()`
   编辑器重启后恢复对旧 PID 的跟踪。

### 10.5 `Editor/ServerMonitorWindow.cs`

关键函数：

1. `OnInspectorUpdate()`
   周期刷新窗口。
2. `DrawOverview(...)`
   展示房间/会话总览。
3. `DrawRoomList(...)` / `DrawSessionList(...)` / `DrawRoomDetails(...)`
   细分查看运行时快照。

数据来源边界：

1. 只读 `ServerApp` 快照接口，不直接修改运行时对象。

### 10.6 `Editor/StellarNetScaffoldWindow.cs`

关键函数：

1. `GenerateScaffold()`
   读取模板并生成模块/组件脚手架。
2. `WriteFile(...)`
   写出目标脚手架文件。

使用边界：

1. 生成的是“起步骨架”，仍需你补协议、处理器和业务逻辑。

### 10.7 `StreamingAssets/StellarNetLiteLoadTest/Program.cs`

关键类型：

1. `LoadTestOptions`
   命令行参数模型与校验入口。
2. `LoadTestRunner`
   总调度器，负责机器人池、命令处理、统计打印。
3. `LoadTestBot`
   单机器人状态机，覆盖登录、建房/加房、进房确认、行为模拟。
4. `LoadTestStats`
   指标聚合器（连接、登录、房间装配、收发包、错误等）。

关键函数示例：

1. `LoadTestRunner.EnqueueCommand(...)` / `ProcessCommands()`
   运行时命令驱动（`addroom/removeroom/endroom/status/help`）。
2. `LoadTestBot.Tick(...)`
   单机器人主循环。
3. `LoadTestBot.SendMove/SendAction/SendBubble`
   行为流量注入点。
4. `LoadTestBot.CheckStateTimeout(...)`
   状态超时与异常回收。

### 10.8 `StreamingAssets/StellarNetLiteLoadTest/Transports.cs`

关键类型：

1. `KcpLoadTestTransport`
   KCP 客户端传输实现，默认只入队控制面 S2C。
2. `TcpLoadTestTransport`
   TCP 客户端传输实现，按长度前缀读完整帧。

关键函数：

1. `Connect/Stop/Send/Pump/TryDequeue`
   压测端统一传输接口实现。
2. `ReadExactAsync(...)`（TCP）
   固定长度读取，避免半帧进入上层解析。
