# 16. StellarNet Lite Extensions 源码逐层参考

本文档覆盖 `StellarNetLite/Extensions`。

理解 `Extensions` 的关键前提：

- `Runtime` 解决“框架骨架”。
- `Extensions` 解决“官方默认业务能力”。
- 这层代码大多通过三种方式接入 Runtime：
  - `ServerModule` / `ClientModule`
  - `ServerRoomComponent` / `ClientRoomComponent`
  - `RuntimeFeatureBridgeBase`

---

## 1. DefaultGameFlow

这一组脚本负责默认登录、大厅、建房、加房、离房、挂起房间、重连。

### 1.1 `DefaultGameFlow/Runtime/DefaultGameFlowRuntimeFeature.cs`

文件职责：

- 默认游戏流程运行时桥。
- 把“登录前豁免协议、踢下线通知、在线列表刷新、重连补登”等流程从 Runtime 主体下沉到扩展层。

成员：

- `OnServerAppCreated`
  把 `DefaultRoomMembershipNotifier.Instance` 挂到 `ServerApp.RoomMembershipNotifier`。
- `OnServerSessionStateChanged`
  会话状态变化时广播大厅在线玩家列表。
- `TryNotifyServerSessionKick`
  服务端踢人前先发 `S2C_KickOut`。
- `OnClientAppCreated`
  注册弱网豁免协议：`C2S_Login`、`C2S_ConfirmReconnect`、`C2S_ReconnectReady`。
- `OnClientConnected`
  如果当前处于重连流程且有 `AccountId`，自动补发登录请求。

### 1.2 `DefaultGameFlow/Server/Infrastructure/DefaultRoomMembershipNotifier.cs`

文件职责：

- 默认房间成员通知器。
- 当房间因为超时清理、强制结算等原因把玩家移出时，默认发送 `S2C_LeaveRoomResult`。

成员：

- `Instance`
  单例。
- `NotifyForcedLeave(Room room, Session session)`
  调用 `room.SendMessageTo` 通知目标成员。

### 1.3 `DefaultGameFlow/Server/Modules/ServerUserModule.cs`

文件职责：

- 服务端用户鉴权与重连模块。

核心字段：

- `_app`
  当前 `ServerApp`。

核心处理函数：

- `OnC2S_Login`
  处理登录：
  - 校验 `AccountId`
  - 校验 `ClientVersion`
  - 处理同账号重登/顶号
  - 旧会话重绑连接
  - 首次登录创建新鉴权会话
  - 决定是否存在可恢复房间
- `OnC2S_ConfirmReconnect`
  决定客户端是否接受恢复房间。
- `OnC2S_ReconnectReady`
  客户端重连装配完成后真正恢复房间并补发快照。
- `TryGetRecoverableRoom`
  判断会话当前能否恢复到某个房间，同时清理失效恢复上下文。

为什么这个模块重要：

- 账号唯一性、顶号、重绑物理连接、恢复房间都在这里。
- 生产环境大多数“重连怎么失效了”的问题都从这里开始查。

### 1.4 `DefaultGameFlow/Server/Modules/ServerRoomModule.cs`

文件职责：

- 服务端房间生命周期模块。

核心字段：

- `_app`
  当前 `ServerApp`。

关键处理函数：

- `OnC2S_CreateRoom`
  建房：
  - 校验当前不在房间内
  - 校验 `RoomDTO`
  - 去重组件 ID
  - 生成房间 ID
  - 创建房间实例
  - 写入 `RoomConfigModel`
  - 调用 `ServerRoomFactory.BuildComponents`
  - 授权当前会话后续确认进入该房间
- `OnC2S_JoinOrCreateRoom`
  如果房间存在则授权加入，不存在则按给定 `RoomId` 降级建房。
- `OnC2S_JoinRoom`
  加房授权：
  - 校验房间是否存在、是否结束、是否满员、密码是否正确
  - 支持“恢复同一个挂起房间”的特殊路径
- `OnC2S_RoomSetupReady`
  最终进房确认：
  - 校验 `AuthorizedRoomId`
  - 处理新进房与恢复进房两条链
  - 对恢复链走 `room.ResumeMember`
  - 对新进房走 `room.TryAddMember`
- `OnC2S_DisconnectRoom`
  挂起当前房间，保留恢复上下文。
- `OnC2S_LeaveRoom`
  离房，必要时销毁空房。
- `DeduplicateComponentIds`
  去重组件 ID 列表。
- `BroadcastRoomListToLobby`
  向所有大厅在线玩家刷新房间列表。
- `CleanupPreviousRecoverableRoom`
  用户进入新房间时清理旧恢复房间上下文。

### 1.5 `DefaultGameFlow/Server/Modules/ServerLobbyModule.cs`

文件职责：

- 服务端大厅信息与全局社交模块。

核心函数：

- `OnC2S_GetRoomList`
  返回当前大厅房间列表。
- `OnC2S_GlobalChat`
  处理大厅聊天并广播。
- `BuildRoomListResponse`
  把 `RoomRuntimeSnapshot` 转成 `RoomBriefInfo[]`。
- `BuildOnlinePlayerListResponse`
  把所有 `Session` 转成 `OnlinePlayerInfo[]`。
- `BroadcastOnlinePlayerList`
  向在线玩家全量推送在线列表。
- `BroadcastPlayerStateChange`
  向在线玩家增量广播某个玩家状态。
- `BroadcastAnnouncement`
  广播系统公告。
- `BuildPlayerInfo`
  统一构造大厅玩家摘要对象。

### 1.6 `DefaultGameFlow/Client/Modules/ClientUserModule.cs`

文件职责：

- 客户端用户流程模块。

关键处理函数：

- `OnS2C_LoginResult`
  登录成功后写入 `ClientSession`，并决定是否进入重连链。
- `OnS2C_ReconnectResult`
  恢复房间成功时创建 `ClientRoom` 并装配组件，随后发送 `C2S_ReconnectReady`。
- `OnS2C_KickOut`
  被顶号/踢下线时离房、清理会话并广播本地提示。

### 1.7 `DefaultGameFlow/Client/Modules/ClientRoomModule.cs`

文件职责：

- 客户端房间生命周期模块。

关键处理函数：

- `OnS2C_CreateRoomResult`
  创建本地房间、装配组件、发送 `C2S_RoomSetupReady`。
- `OnS2C_JoinRoomResult`
  加房成功后的本地装配链。
- `OnS2C_LeaveRoomResult`
  清理恢复上下文并离房。
- `OnS2C_DisconnectRoomResult`
  挂起房间成功后回大厅并提示可以恢复。
- `OnS2C_RoomSetupResult`
  最终确认进房成功后调用 `ClientApp.ConfirmCurrentRoom`。

### 1.8 `DefaultGameFlow/Client/Modules/ClientLobbyModule.cs`

文件职责：

- 客户端大厅模块。
- 主要做“把大厅协议转成本地事件”。

关键处理函数：

- `OnS2C_RoomListResponse`
- `OnS2C_OnlinePlayerListSync`
- `OnS2C_GlobalPlayerStateIncrementalSync`
- `OnS2C_GlobalAnnouncement`
  同时转换成本地 `Local_SystemPrompt`。
- `OnS2C_GlobalChatSync`

### 1.9 `DefaultGameFlow/Shared/Protocol/Module/UserProtocols.cs`

协议职责：

- 登录、踢下线、重连确认、重连最终装配。

类型与字段：

- `C2S_Login`
  - `AccountId`
  - `ClientVersion`
- `S2C_LoginResult`
  - `Success`
  - `SessionId`
  - `HasReconnectRoom`
  - `Reason`
- `S2C_KickOut`
  - `Reason`
- `C2S_ConfirmReconnect`
  - `Accept`
- `S2C_ReconnectResult`
  - `Success`
  - `RoomId`
  - `ComponentIds`
  - `Reason`
- `C2S_ReconnectReady`
  空消息，表示客户端恢复装配完成。

### 1.10 `DefaultGameFlow/Shared/Protocol/Module/RoomScheduleProtocols.cs`

协议职责：

- 建房、加房、离房、挂起房间、房间最终确认。

类型与字段：

- `RoomDTO`
  - `RoomName`
  - `ComponentIds`
  - `MaxMembers`
  - `EnableReplayRecording`
  - `Password`
  - `CustomProperties`
- `C2S_CreateRoom`
  - `RoomConfig`
- `S2C_CreateRoomResult`
  - `Success`
  - `RoomId`
  - `ComponentIds`
  - `Reason`
- `C2S_JoinRoom`
  - `RoomId`
  - `Password`
- `S2C_JoinRoomResult`
  - `Success`
  - `RoomId`
  - `ComponentIds`
  - `Reason`
- `C2S_LeaveRoom`
  空消息。
- `S2C_LeaveRoomResult`
  - `Success`
- `C2S_DisconnectRoom`
  请求挂起当前房间。
- `S2C_DisconnectRoomResult`
  - `Success`
  - `RoomId`
  - `Reason`
- `C2S_RoomSetupReady`
  - `RoomId`
- `C2S_JoinOrCreateRoom`
  - `RoomId`
  - `RoomConfig`
- `S2C_RoomSetupResult`
  - `Success`
  - `RoomId`
  - `Reason`

### 1.11 `DefaultGameFlow/Shared/Protocol/Module/LobbyProtocols.cs`

协议职责：

- 大厅房间列表、在线玩家列表、增量玩家状态、公告、聊天。

类型与字段：

- `RoomBriefInfo`
  - `RoomId`
  - `RoomName`
  - `MemberCount`
  - `MaxMembers`
  - `IsPrivate`
  - `State`
- `OnlinePlayerInfo`
  - `SessionId`
  - `AccountId`
  - `DisplayName`
  - `IsOnline`
  - `IsInRoom`
  - `RoomId`
- `C2S_GetRoomList`
  空消息。
- `S2C_RoomListResponse`
  - `Rooms`
- `S2C_OnlinePlayerListSync`
  - `Players`
- `S2C_GlobalPlayerStateIncrementalSync`
  - `IsRemoved`
  - `Player`
- `S2C_GlobalAnnouncement`
  - `Title`
  - `Content`
  - `PublishUnixTime`
- `C2S_GlobalChat`
  - `Content`
- `S2C_GlobalChatSync`
  - `SenderSessionId`
  - `SenderDisplayName`
  - `Content`
  - `SendUnixTime`

### 1.12 `DefaultGameFlow/Shared/Protocol/Module/GameFlowProtocols.cs`

协议职责：

- 纯对局开局/结束链。

类型与字段：

- `C2S_StartGame`
  房主请求开始。
- `S2C_GameStarted`
  - `StartUnixTime`
- `C2S_EndGame`
  房主请求结束。
- `S2C_GameEnded`
  - `WinnerSessionId`
  - `ReplayId`

---

## 2. RoomFlow

这一组脚本负责“房间成员基础流程”，与 `DefaultGameFlow` 的“房间生命周期”配合使用。

### 2.1 `RoomFlow/Server/Components/ServerRoomSettingsComponent.cs`

文件职责：

- 服务端房间基础设置组件。
- 负责房主、成员准备状态、成员快照、开局/结束控制。

核心字段：

- `_app`
  当前 `ServerApp`。
- `_readyStates`
  `SessionId -> IsReady`。
- `_ownerSessionId`
  当前房主。

关键函数：

- `OnInit`
  初始化房主和 ready 状态缓存。
- `CreateMemberInfo`
  组装 `MemberInfo`。
- `OnMemberJoined`
  维护房主、ready 状态并广播加入事件。
- `OnMemberLeft`
  删除 ready 状态并必要时迁移房主。
- `OnMemberOffline`
  房主离线时触发房主迁移。
- `MigrateHost`
  在剩余成员中优先选择在线成员成为房主。
- `OnSendSnapshot`
  给指定成员补发完整房间快照；如果房间已经开局，还会补 `S2C_GameStarted`。
- `BroadcastSnapshotToAll`
  向全体广播完整房间快照。
- `OnC2S_SetReady`
  更新成员 ready 状态并广播。
- `OnC2S_StartGame`
  仅房主且所有其他成员 ready 时允许开局。
- `OnC2S_EndGame`
  仅房主且当前在 `Playing` 态时允许强制结束。

### 2.2 `RoomFlow/Client/Components/ClientRoomSettingsComponent.cs`

文件职责：

- 客户端房间基础信息缓存组件。

核心字段/属性：

- `_app`
  所属 `ClientApp`。
- `Members`
  房间成员缓存表。
- `IsGameStarted`
  当前是否已开局。
- `RoomName`
  房间名。
- `MaxMembers`
  最大人数。
- `IsPrivate`
  是否私有房。

关键函数：

- `OnInit`
  重置本地缓存。
- `OnDestroy`
  清空成员表。
- `OnS2C_RoomSnapshot`
  覆盖整个本地房间快照。
- `OnS2C_MemberJoined`
  增量加入成员。
- `OnS2C_MemberLeft`
  增量移除成员。
- `OnS2C_MemberReadyChanged`
  更新成员 ready 状态。
- `OnS2C_GameStarted`
  标记开局。
- `OnS2C_GameEnded`
  标记结束并重置全部 ready 状态。

### 2.3 `RoomFlow/Shared/Protocol/Components/RoomSettingsProtocols.cs`

协议职责：

- 房间成员快照、成员加入离开、ready 同步。

类型与字段：

- `MemberInfo`
  - `SessionId`
  - `AccountId`
  - `DisplayName`
  - `IsReady`
  - `IsOwner`
- `S2C_RoomSnapshot`
  - `RoomName`
  - `MaxMembers`
  - `IsPrivate`
  - `Members`
- `S2C_MemberJoined`
  - `Member`
- `S2C_MemberLeft`
  - `SessionId`
- `C2S_SetReady`
  - `IsReady`
- `S2C_MemberReadyChanged`
  - `SessionId`
  - `IsReady`

---

## 3. NetworkMonitoring

这一组脚本负责 Ping、RTT、弱网告警、阻断与主动熔断。

### 3.1 `NetworkMonitoring/Runtime/NetworkMonitoringRuntimeFeature.cs`

文件职责：

- 弱网监控运行时桥。

成员：

- `_networkMonitor`
  当前挂在 `StellarNetAppManager` 上的 `ClientNetworkMonitor`。
- `OnClientAppCreated`
  注册 `C2S_Ping` 弱网豁免，创建并初始化监控器。
- `OnClientPacketReceived`
  任意收包都刷新监控器活跃时间。
- `OnClientStopped`
  清空监控器引用。

### 3.2 `NetworkMonitoring/Client/Infrastructure/ClientNetworkMonitor.cs`

文件职责：

- 客户端弱网监控器。

核心字段：

- `_app`
  当前 `ClientApp`。
- `_transport`
  当前物理传输层。
- `_isWarnTriggered`
  是否进入弱网告警态。
- `_isBlockTriggered`
  是否进入弱网阻断态。
- `_blockDuration`
  已连续阻断时长。
- `_uiRefreshTimer`
  UI 事件节流计时器。
- `_pingTimer`
  主动发 Ping 节流计时器。
- `_currentRttMs`
  当前 RTT。
- `_lastPongReceiveTime`
  最近收到 Pong 或任意网络包的时间。
- `WeakNetWarnRttMs`
  进入告警态阈值。
- `WeakNetBlockRttMs`
  进入阻断态阈值。
- `ActiveFuseTimeoutSeconds`
  阻断持续太久时主动断线阈值。

关键函数：

- `Init`
  绑定 `ClientApp` 与 transport，订阅 `Local_PingResult`。
- `OnPingResult`
  用 Pong 回来的时间戳计算 RTT。
- `OnPacketReceived`
  任意收包都刷新活动时间。
- `Update`
  每帧执行监控逻辑：
  - 非在线房间态时重置状态
  - 周期性发送 `C2S_Ping`
  - 超过阈值进入 warn/block
  - 阻断持续过久则主动 `_transport.StopClient()`
  - 广播 `Local_NetworkQualityChanged`
  - 更新 `ClientApp.IsNetworkBlocked`

### 3.3 `NetworkMonitoring/Client/Modules/ClientPingModule.cs`

文件职责：

- 把 `S2C_Pong` 转成本地 RTT 事件。

成员：

- `_app`
  当前 `ClientApp`。
- `OnS2C_Pong`
  通过 `Time.realtimeSinceStartup - msg.ClientTime` 计算 RTT，并广播 `Local_PingResult`。

### 3.4 `NetworkMonitoring/Server/Modules/ServerPingModule.cs`

文件职责：

- 服务端心跳回包模块。

成员：

- `_app`
  当前 `ServerApp`。
- `OnC2S_Ping`
  原样回送 `S2C_Pong.ClientTime`。

### 3.5 `NetworkMonitoring/Shared/Protocol/Module/PingProtocols.cs`

类型与字段：

- `C2S_Ping`
  - `ClientTime`
- `S2C_Pong`
  - `ClientTime`

### 3.6 `NetworkMonitoring/Client/Events/NetworkMonitoringLocalEvents.cs`

本地事件：

- `Local_NetworkQualityChanged`
  - `RttMs`
  - `IsWeakNetWarn`
  - `IsWeakNetBlock`
- `Local_PingResult`
  - `RttMs`

---

## 4. ObjectSync

这一组脚本负责网络对象的创建、销毁、变换同步、动画同步、回放快照导入导出。

### 4.1 `ObjectSync/Runtime/ObjectSyncRuntimeFeature.cs`

文件职责：

- ObjectSync 运行时桥。

成员：

- `OnRuntimeAwake`
  预加载 `ObjectSync` 配置。

### 4.2 `ObjectSync/Shared/Infrastructure/ObjectSyncConfig.cs`

类型与字段：

- `ObjectSyncGlobalConfig`
  - `ReplayObjectSyncRecordIntervalTicks`
    回放记录对象同步快照的 tick 间隔。
  - `ObjectSyncOnlineIntervalTicks`
    在线同步间隔。
  - `EnableAdaptiveObjectSync`
    是否按房间人数启用自适应同步频率。
  - `ObjectSyncFullResyncIntervalTicks`
    全量重同步间隔。
- `ObjectSyncConfigLoader`
  - `ConfigFileName`
  - `_cachedConfig`
  - `Current`
  - `LoadRuntimeConfigSync/Async`

### 4.3 `ObjectSync/Shared/Protocol/Components/ObjectSync/ObjectSpawnState.cs`

文件职责：

- 单个网络对象出生快照。
- 实现 `ILiteNetSerializable`，属于高频/大体积二进制结构。

字段：

- `NetId`
- `PrefabHash`
- `Mask`
- `PosX/PosY/PosZ`
- `RotX/RotY/RotZ`
- `DirX/DirY/DirZ`
- `ScaleX/ScaleY/ScaleZ`
- `AnimStateHash`
- `AnimNormalizedTime`
- `AnimParamCount`
- `AnimParams`
- `OwnerSessionId`

### 4.4 `ObjectSync/Shared/Protocol/Components/ObjectSync/ObjectSyncProtocols.cs`

文件职责：

- 定义对象同步脏位、状态结构和消息协议。

核心类型：

- `EntitySyncMask`
  描述对象具备哪些同步能力，如 transform/animator/owner 等。
- `ObjectSyncDirtyMask`
  描述本帧哪些字段发生变化。
- `ObjectSyncState`
  单帧对象同步增量，字段包括：
  - `NetId`
  - `Mask`
  - `DirtyMask`
  - `PosX/PosY/PosZ`
  - `RotX/RotY/RotZ`
  - `VelX/VelY/VelZ`
  - `ScaleX/ScaleY/ScaleZ`
  - `AnimStateHash`
  - `AnimNormalizedTime`
  - `AnimParamCount`
  - `AnimParams`
- `S2C_ObjectSpawn`
  出生消息，内部持有 `ObjectSpawnState State`，并提供一组只读转发表达属性。
- 其余同文件协议通常包括：
  - `S2C_ObjectDestroy`
  - `S2C_ObjectSync`
  - 与回放快照/重同步相关的同步载体

这个文件是对象同步协议层核心，和 `ServerObjectSyncComponent` / `ClientObjectSyncComponent` 强绑定。

### 4.5 `ObjectSync/Server/Components/ServerObjectSyncComponent.cs`

文件职责：

- 服务端对象同步核心组件。

核心类型与字段：

- `ServerSyncEntity`
  描述单个服务端权威实体：
  - `NetId`
  - `PrefabHash`
  - `Mask`
  - `OwnerSessionId`
  - `Position`
  - `Rotation`
  - `Velocity`
  - `Scale`
  - `AnimStateHash`
  - `AnimNormalizedTime`
  - `AnimParams`
  - `AnimParamCount`
- `SyncSnapshot`
  上次发送基线，用于脏位计算。
- `ServerObjectSyncComponent`
  - `_app`
    当前 `ServerApp`
  - `_entities`
    权威实体表
  - `_lastSentSnapshots`
    上次发送基线表
  - `_netIdCounter`
    新实体 ID 自增器
  - 房间规模阈值：
    `MediumRoomMemberThreshold`、`LargeRoomMemberThreshold`
  - 动态同步间隔：
    `MediumRoomIntervalTicks`、`LargeRoomIntervalTicks`
  - 脏位阈值：
    `PositionDirtyThresholdSqr`、`RotationDirtyThreshold`、`VelocityDirtyThresholdSqr`、`ScaleDirtyThresholdSqr`、`FloatParamDirtyThreshold`、`AnimNormalizedTimeDirtyThreshold`
  - `_syncStateBuffer`
    复用的同步状态缓存
  - `_reusableSyncMsg`
    复用的同步消息对象
  - `SnapshotComponentId`
    该组件在回放快照中的组件 ID，当前为 `200`

职责拆解：

- 生成/销毁网络实体
- 维护权威 transform/velocity/animator 状态
- 按阈值算脏位
- 周期性广播增量或全量同步
- 导出对象同步回放快照

### 4.6 `ObjectSync/Client/Components/ClientObjectSyncComponent.cs`

文件职责：

- 客户端对象同步消费核心。

核心类型与字段：

- `PredictedTransformData`
  - `Position`
  - `Rotation`
  - `Velocity`
  - `Scale`
  - `TimeSinceLastSync`
  - `PlaybackSpeed`
- `PredictedAnimatorData`
  - `AnimStateHash`
  - `AnimNormalizedTime`
  - `AnimParamCount`
  - `AnimParams`
  - `PlaybackSpeed`
  - `ServerTimeDelta`
- `SyncEntityData`
  当前客户端缓存的单个实体同步状态：
  - `PrefabHash`
  - `Mask`
  - `OwnerSessionId`
  - `RawPos/RawRot/RawVel/RawScale`
  - `AnimStateHash`
  - `AnimNormalizedTime`
  - `AnimParamCount`
  - `AnimParams`
  - `LocalReceiveTime`
  - `ServerTime`
- `ClientObjectSyncComponent`
  - `_app`
  - `RemoteOnlinePredictionLeadSeconds`
  - `MaxOnlinePredictionWindow`
  - `_entities`
    客户端同步缓存
  - `_replayTimeScale`
  - `_timeScaleEventToken`
  - `_replayBaseLocalTime`
  - `_replayBaseServerTime`
  - `SnapshotComponentId`

职责拆解：

- 消费服务端对象出生/销毁/同步消息
- 为本地视图层提供预测数据
- 消费回放快照
- 响应回放倍速变化

### 4.7 `ObjectSync/Client/Runtime/ObjectSyncClientRuntime.cs`

文件职责：

- 负责把“同步数据”转成“Unity 物体实例与视图绑定”。

类型与成员：

- `NetEntityLoadState`
  描述网络实体加载结果。
- 与加载结果相关的只读属性：
  `Success`、`Prefab`、`ErrorMessage`、`Room`、`SyncService`、`State`、`RootTransform`
- `INetPrefabResolver`
  如何根据 `PrefabHash` 找到预制体。
- `INetSpawnStrategy`
  如何实例化和销毁对象。
- `INetViewBinder`
  如何把 `NetIdentity`、`NetTransformView`、`NetAnimatorView` 绑定到实例。
- `ObjectSyncClientRuntime`
  全局入口，持有：
  - `_prefabResolver`
  - `_spawnStrategy`
  - `_viewBinder`
  - 默认访问器：`PrefabResolver`、`SpawnStrategy`、`ViewBinder`
- `ResourcesNetPrefabResolver`
  默认 `Resources` 预制体解析器，内部有 `_cache`。
- `DefaultNetSpawnStrategy`
  默认实例化/销毁策略。
- `DefaultNetViewBinder`
  默认视图组件绑定策略。

### 4.8 `ObjectSync/Client/Views/NetIdentity.cs`

文件职责：

- 视图对象与同步服务之间的身份桥。

成员：

- `NetId`
  当前实例的网络 ID。
- `SyncService`
  所属 `ClientObjectSyncComponent`。

### 4.9 `ObjectSync/Client/Views/NetTransformView.cs`

文件职责：

- 把同步层的位置/朝向/速度平滑应用到 Unity Transform。

字段：

- `PosSmoothTime`
- `RotSmoothSpeed`
- `SnapThreshold`
- `CatchUpThreshold`
- `StopThreshold`
- `LocalSnapThreshold`
- `LocalSoftCorrectionSpeed`
- `LocalIgnoreThreshold`
- `IsLocalPlayer`
- `_identity`
- `_currentVelocity`
- `_characterController`

### 4.10 `ObjectSync/Client/Views/NetAnimatorView.cs`

文件职责：

- 把同步层的动画状态应用到 `Animator`。

关键字段：

- `TargetAnimator`
- `ParamSmoothTime`
- `AnimCrossFadeTime`
- `EnableAntiIceSkating`
- `AntiIceTargetStates`
- `AntiIceSourceStates`
- `SyncedStates`
- `SyncedFloatParams`
- `IsLocalPlayer`
- `_identity`
- `_lastAnimStateHash`
- `_antiIceTargetHashes`
- `_antiIceSourceHashes`
- `_logicToAnimatorStateHashes`
- `_logicToAnimatorParamHashes`
- `_animatorFloatParamHashes`
- `_unknownSyncedStateHashes`
- `_unknownSyncedParamHashes`
- `_receivedAnimatorParamHashes`
- `_initialAnimatorParamValues`
- `_currentParamValues`
- `_paramVelocities`

这一版 `NetAnimatorView` 已经不是旧的“固定 3 个 float 参数 + 字符串状态列表”方案，而是完整升级为：

1. `SyncedStates`
   可视化的状态映射列表，每项包含：
   - `Enabled`
   - `LogicStateName`
   - `AnimatorStateName`
2. `SyncedFloatParams`
   可视化的动态 float 参数映射列表，每项包含：
   - `Enabled`
   - `LogicParamName`
   - `AnimatorParamName`
3. Inspector 自动扫描
   从 `Animator Controller` 自动列出所有状态和 float 参数。
4. Inspector 校验
   会直接提示状态不存在、参数不存在、重复逻辑名、反滑步列表填错等问题。
5. 运行时动态参数恢复
   如果服务端本帧没有继续发送某个已映射 float 参数，客户端会把该参数平滑恢复到 Animator 初始值，避免旧参数残留造成 BlendTree 误判。

动画同步的核心运行时职责如下：

1. `HardSetInitialState(...)`
   实体首次生成时，立即应用初始动画状态和参数。
2. `ProcessAnimatorSync(...)`
   每帧读取 `ClientObjectSyncComponent` 提供的预测动画数据。
3. `ResolveAnimatorStateHash(...)`
   把服务端逻辑状态 hash 映射成 Animator 真正可播放的状态 hash。
4. `ApplyAnimatorParams(...)`
   把服务端动态参数数组映射到 Animator float 参数，并进行平滑。
5. `ResetMissingAnimatorParams(...)`
   把本帧未收到的已映射参数回收到默认值。
6. `ShouldBlockTransitionIntoStopState(...)`
   反滑步判断，避免实体还在移动时就切进 `Idle`。

### 4.11 `ObjectSync/Client/Views/ObjectSpawnerView.cs`

文件职责：

- 客户端网络实体生成与销毁视图枢纽。

内部类型：

- `SpawnedEntityEntry`
  - `LatestState`
  - `Instance`
  - `LoadCts`
  - `LoadTask`
  - `LoadState`

关键字段：

- `_room`
- `_syncService`
- `_spawnEntries`
- `_isInitialized`
- `IsInitialized`

作用：

- 根据 `ObjectSpawnState` 请求加载/创建对象
- 记录当前已生成实体
- 管理异步加载生命周期

---

## 5. Replay

这一组脚本负责录像录制、存储、分块下载与客户端回放。

### 5.1 `Replay/Runtime/ReplayRuntimeFeature.cs`

文件职责：

- 回放运行时桥。

成员：

- `OnRuntimeAwake`
  加载回放配置并初始化录像目录。
- `OnServerAppCreated`
  把 `ReplayRoomRecordingService.Instance` 注入到 `ServerApp.RoomRecordingService`。

### 5.2 `Replay/Shared/Infrastructure/ReplayConfig.cs`

类型与字段：

- `ReplayGlobalConfig`
  - `MaxReplayFiles`
  - `EnableReplayRecording`
- `ReplayConfigLoader`
  - `ConfigFileName`
  - `_cachedConfig`
  - `Current`
  - 负责加载并缓存回放配置

### 5.3 `Replay/Shared/Replay/ReplaySnapshotDefines.cs`

文件职责：

- 定义回放文件格式常量、快照接口和快照帧数据。

类型与成员：

- `ReplayFrameKind`
  区分消息帧、快照帧等回放帧类型。
- `ReplayFormatDefines`
  - `MagicBytes`
  - `VersionLegacy`
  - `VersionWithObjectSnapshot`
  - `VersionWithTickRate`
  - `DefaultTotalTicksFallback`
  - `DefaultTickRateFallback`
- `IReplaySnapshotProvider`
  房间组件导出回放快照接口。
- `IReplaySnapshotConsumer`
  客户端组件消费回放快照接口。
- `ComponentSnapshotData`
  - `ComponentId`
  - `Payload`
- `ReplaySnapshotFrame`
  - `Tick`
  - `ComponentSnapshots`

### 5.4 `Replay/Server/Infrastructure/ReplayRoomRecordingService.cs`

文件职责：

- `Room` 所依赖的录像录制服务实现。

成员：

- `ReplaySnapshotIntervalTicks`
  周期快照间隔。
- `Instance`
  单例。

核心作用：

- 判断某房间能否开始录制
- 开始/终止录制
- 记录初始快照、周期快照、消息帧、结束快照
- 最终调用 `ServerReplayStorage` 落盘

### 5.5 `Replay/Shared/Infrastructure/ServerReplayStorage.cs`

文件职责：

- 回放文件真正的落盘与写线程实现。

核心常量与字段：

- `ReplayFolderName`
- `MessageFrameMsgIdPlaceholder`
- `FrameHeaderBytes`
- `_replayFolderPath`
- `ActiveRecords`
  当前所有正在写的回放记录上下文。

内部类型：

- `ReplayWriteItem`
  - `FrameKind`
  - `Tick`
  - `MsgId`
  - `PayloadBuffer`
  - `PayloadLength`
- `RecordContext`
  - `FS`
  - `GZ`
  - `Writer`
  - `TempFilePath`
  - `PendingWrites`
  - `WorkerTask`
  - `WorkerException`
  - `PendingBytes`

作用：

- 统一初始化回放目录
- 管理单房间录制上下文
- 用阻塞队列驱动写线程
- 控制压缩、临时文件与最终文件切换

### 5.6 `Replay/Server/Modules/ServerReplayModule.cs`

文件职责：

- 服务端回放下载与重命名模块。

类型与字段：

- `ReplayDownloadTask`
  - `ReplayId`
  - `FS`
  - `TotalLength`
  - `CurrentOffset`
  - `LastActiveTimeUtc`
- `ServerReplayModule`
  - `_app`
  - `ChunkSize`
  - `_downloadTasks`

核心职责：

- 处理回放列表请求
- 处理回放下载初始化
- 按块发送 `S2C_DownloadReplayChunk`
- 处理 `C2S_DownloadReplayChunkAck`
- 回收超时下载任务
- 处理回放重命名

### 5.7 `Replay/Client/Core/ClientReplayPlayer.cs`

文件职责：

- 客户端回放播放器。

关键字段：

- `_app`
- `CurrentTick`
- `PlaybackSpeed`
- `IsPaused`
- `_isPlaying`
- `_tickAccumulator`
- `_tickInterval`
- `_lastReportedTimeScale`
- `_replayFilePath`
- `_rawFilePath`
- `_rawFs`
- `_rawReader`
- `_roomId`
- `_componentIds`
- `_realTotalTicks`
- `_recordedTickRate`
- `_replayVersion`
- `_messageFrameIndex`
- `_snapshotFrameIndex`
- `SparseIndexIntervalTicks`
- `_nextFrame`

核心职责：

- 打开回放文件
- 解析回放头和帧索引
- 推进 `CurrentTick`
- 给房间组件投递消息帧
- 给实现 `IReplaySnapshotConsumer` 的组件投递快照帧
- 广播回放倍速变化

### 5.8 `Replay/Client/Modules/ClientReplayModule.cs`

文件职责：

- 客户端回放下载与事件转发模块。

关键字段：

- `_app`
- `_connectionAbortedToken`
- `_downloadingReplayId`
- `_expectedTotalBytes`
- `_fileStream`
- `_pendingFlushBytes`
- `FlushThresholdBytes`
- `CacheFolderPath`

关键职责：

- 接收回放列表并广播给 UI
- 接收下载开始/下载分块/下载完成
- 累积写本地缓存文件
- 周期性 flush，避免每个分块都刷盘
- 连接中断时清理未完成下载

### 5.9 `Replay/Client/Events/ReplayLocalEvents.cs`

本地事件：

- `Local_ReplayTimeScaleChanged`
  - `TimeScale`
- `Local_ReplayDownloadProgress`
  - `ReplayId`
  - `DownloadedBytes`
  - `TotalBytes`

### 5.10 `Replay/Shared/Protocol/ReplayProtocols.cs`

协议职责：

- 回放列表、下载、分块传输、重命名。

类型与字段：

- `ReplayBriefInfo`
  - `ReplayId`
  - `DisplayName`
  - `Timestamp`
  - `TotalTicks`
- `C2S_GetReplayList`
  空消息。
- `S2C_ReplayList`
  - `Replays`
- `C2S_DownloadReplay`
  - `ReplayId`
  - `StartOffset`
- `S2C_DownloadReplayResult`
  - `Success`
  - `ReplayId`
  - `ReplayFileData`
  - `Reason`
- `S2C_DownloadReplayStart`
  - `Success`
  - `ReplayId`
  - `TotalBytes`
  - `AcceptedOffset`
  - `Reason`
- `S2C_DownloadReplayChunk`
  - `ReplayId`
  - `ChunkData`
- `C2S_DownloadReplayChunkAck`
  - `ReplayId`
- `C2S_RenameReplay`
  - `ReplayId`
  - `NewName`

---

## 6. 这一层的阅读建议

如果你要快速掌握默认业务主链，建议顺序是：

1. `DefaultGameFlow/Server/Modules/ServerUserModule.cs`
2. `DefaultGameFlow/Server/Modules/ServerRoomModule.cs`
3. `DefaultGameFlow/Client/Modules/ClientUserModule.cs`
4. `DefaultGameFlow/Client/Modules/ClientRoomModule.cs`
5. `RoomFlow/Server/Components/ServerRoomSettingsComponent.cs`
6. `RoomFlow/Client/Components/ClientRoomSettingsComponent.cs`

如果你要查同步、回放或高负载问题，建议顺序是：

1. `ServerObjectSyncComponent.cs`
2. `ClientObjectSyncComponent.cs`
3. `ReplayRoomRecordingService.cs`
4. `ServerReplayStorage.cs`
5. `ClientReplayPlayer.cs`

如果你要查弱网或连接降级问题，建议顺序是：

1. `ClientNetworkMonitor.cs`
2. `ClientPingModule.cs`
3. `NetworkMonitoringRuntimeFeature.cs`

---

## 7. 默认业务模块协作时序（登录到进房）

这一节把 `DefaultGameFlow + RoomFlow` 按执行顺序串起来，便于新手定位“某一步该看谁”。

### 7.1 登录链

1. 客户端发 `C2S_Login`（`UserProtocols.cs`）。
2. `ServerUserModule.OnC2S_Login(...)` 完成鉴权、版本检查、顶号/重绑处理。
3. 服务端回 `S2C_LoginResult`。
4. `ClientUserModule.OnS2C_LoginResult(...)` 更新 `ClientSession`。

### 7.2 建房/加房链

1. 客户端发 `C2S_CreateRoom` 或 `C2S_JoinRoom/C2S_JoinOrCreateRoom`（`RoomScheduleProtocols.cs`）。
2. `ServerRoomModule` 完成房间创建或加入授权。
3. 服务端回 `S2C_CreateRoomResult` 或 `S2C_JoinRoomResult`（带 `ComponentIds`）。
4. `ClientRoomModule` 创建本地 `ClientRoom` 并调用组件装配。
5. 客户端发 `C2S_RoomSetupReady`。
6. `ServerRoomModule.OnC2S_RoomSetupReady(...)` 执行最终进房确认并回 `S2C_RoomSetupResult`。

### 7.3 房间设置链（RoomFlow）

1. `ServerRoomSettingsComponent` 管理准备状态、开局条件和房间设置广播。
2. `ClientRoomSettingsComponent` 消费房间设置与准备状态同步。
3. 相关协议定义在 `RoomSettingsProtocols.cs`。

阅读建议：若“能登录但开局按钮无效”，优先排查 `ServerRoomSettingsComponent` 与对应 `C2S_SetReady/C2S_StartGame` 处理链。

---

## 8. ObjectSync 接入核对表（只用内置组件）

用于确认你是否走在“业务补 C2S，内置发 S2C”的正确路径。

### 8.1 服务端侧

1. 房间组件中通过 `Room.GetComponent<ServerObjectSyncComponent>()` 拿到同步服务。
2. 实体生成调用 `SpawnObject(...)`，写入 `EntitySyncMask`。
3. 客户端请求到达后，只改 `ServerSyncEntity` 字段：
   - `Position/Rotation/Velocity`
   - `SetAnimState(...)`
   - `SetAnimParam(...)`
4. 不自定义 `S2C_ObjectSpawn/S2C_ObjectSync/S2C_ObjectDestroy` 的替代协议。

### 8.2 客户端侧

1. 房间组件中保留内置 `ClientObjectSyncComponent`。
2. 场景放置 `ObjectSpawnerView` 并在进房后 `Init(room)`。
3. 预制体挂 `NetIdentity + NetTransformView + NetAnimatorView`。
4. `NetAnimatorView` 的逻辑状态/参数映射和服务端逻辑名一致。

### 8.3 两条业务分支的边界

1. 客户端请求输入：
   - 客户端发输入意图。
   - 服务端 Tick 计算真实状态并写回实体。
2. 客户端请求位置转发：
   - 客户端发位置/速度结果。
   - 服务端做合法性校验后写回实体。

两条分支最终都必须回到：`ServerSyncEntity` 写入 -> 内置 ObjectSync 广播。

---

## 9. 新手最常见误区（Extensions 视角）

1. 误区：把 `DefaultGameFlow` 当成“必须原样保留”。
   正解：它是默认实现，可在不改 Runtime 的前提下替换或扩展模块。
2. 误区：房间业务全塞进全局模块。
   正解：凡是依赖成员列表/Tick/房间生命周期的逻辑，优先房间组件。
3. 误区：ObjectSync 里再造一套 S2C 同步协议。
   正解：业务只扩 C2S 请求，S2C 主同步链继续使用内置协议。
4. 误区：重连失败先改网络层。
   正解：先查 `ServerUserModule` 与 `ServerRoomModule` 的恢复上下文链路。

---

## 10. 逐脚本字段/函数手册（核心扩展）

### 10.1 `DefaultGameFlow/Server/Modules/ServerUserModule.cs`

字段与职责：

1. `_app`
   服务端全局上下文；会话查询、连接重绑、消息下发都经由它。

关键处理函数：

1. `OnC2S_Login(...)`
   登录入口：账号校验、版本校验、顶号处理、旧会话复用/重绑、首次会话创建。
2. `OnC2S_ConfirmReconnect(...)`
   客户端确认是否恢复房间。
3. `OnC2S_ReconnectReady(...)`
   客户端恢复装配完成后的最终恢复入口。

排错建议：

1. “同账号顶号行为不符合预期”优先查 `OnC2S_Login`。
2. “显示可恢复但恢复失败”优先查 `TryGetRecoverableRoom` 与重连确认分支。

### 10.2 `DefaultGameFlow/Server/Modules/ServerRoomModule.cs`

字段与职责：

1. `_app`
   房间生命周期入口依赖，负责建房、加房、离房、授权和大厅刷新。

关键处理函数：

1. `OnC2S_CreateRoom(...)`
   创建房间并装配 `ComponentIds`。
2. `OnC2S_JoinOrCreateRoom(...)`
   房间存在则加入授权，不存在则按请求降级创建。
3. `OnC2S_JoinRoom(...)`
   显式加房流程（满员/密码/状态校验）。
4. `OnC2S_RoomSetupReady(...)`
   客户端组件装配完成后的最终进房确认。
5. `OnC2S_DisconnectRoom(...)`
   挂起房间并设置恢复上下文。
6. `OnC2S_LeaveRoom(...)`
   离房与空房清理链。

辅助函数：

1. `BroadcastRoomListToLobby()`
   房间状态变化后刷新大厅房间列表。
2. `CleanupPreviousRecoverableRoom(...)`
   新房间生效时清理旧恢复上下文。

### 10.3 `DefaultGameFlow/Client/Modules/ClientUserModule.cs`

关键处理函数：

1. `OnS2C_LoginResult(...)`
   登录成功写 `ClientSession`，必要时进入重连链。
2. `OnS2C_ReconnectResult(...)`
   恢复成功时创建房间并装配组件，然后发 `C2S_ReconnectReady`。
3. `OnS2C_KickOut(...)`
   顶号/踢下线处理，清理本地状态并广播提示。

### 10.4 `DefaultGameFlow/Client/Modules/ClientRoomModule.cs`

关键处理函数：

1. `OnS2C_CreateRoomResult(...)`
   建房成功后的本地进房与组件装配。
2. `OnS2C_JoinRoomResult(...)`
   加房成功后的本地进房与组件装配。
3. `OnS2C_LeaveRoomResult(...)`
   清恢复上下文并离房。
4. `OnS2C_DisconnectRoomResult(...)`
   挂起成功后返回大厅并提示可恢复。
5. `OnS2C_RoomSetupResult(...)`
   终态确认；成功则 `ConfirmCurrentRoom`，失败则回退。

### 10.5 `ObjectSync/Server/Components/ServerObjectSyncComponent.cs`

关键类型 `ServerSyncEntity`：

1. Transform 相关：`Position/Rotation/Velocity/Scale`
2. Animator 相关：`AnimStateHash/AnimNormalizedTime/AnimParams/AnimParamCount`
3. 所有权相关：`OwnerSessionId`

关键函数：

1. `OnInit()`
   初始化实体容器与缓存结构。
2. `OnTick()`
   脏检查、增量同步、定期间隔控制与全量重同步调度。
3. `OnMemberJoined(Session)`
   新成员加入时补发当前实体快照。
4. `DestroyObject(int netId)`
   销毁实体并下发销毁同步。
5. `OnSendSnapshot(Session)`
   对单会话补发快照（重连/恢复常用）。

实体动画写入入口：

1. `SetAnimState(...)`
2. `SetAnimParam(...)`
3. `ClearAnimParams()`

### 10.6 `ObjectSync/Client/Components/ClientObjectSyncComponent.cs`

关键处理函数：

1. `OnInit()` / `OnDestroy()`
   订阅与解除本地事件、初始化缓存。
2. `OnS2C_ObjectSpawn(...)`
   处理实体出生同步。
3. `OnS2C_ObjectDestroy(...)`
   处理实体销毁同步。
4. `OnS2C_ObjectSync(...)`
   处理增量状态同步。
5. `ApplyLiveState(...)`
   把实时增量写入客户端实体缓存。
6. `ApplySnapshot(...)` / `ApplyReplaySnapshot(...)`
   回放链路下的状态恢复。
7. `ClearAllEntities(...)`
   清空当前缓存并按需广播销毁事件。

### 10.7 `ObjectSync/Client/Views/ObjectSpawnerView.cs`

关键字段：

1. `_room` / `_syncService`
   当前绑定房间与同步组件。
2. `_spawnEntries`
   已生成实体与加载状态表。
3. `_isInitialized`
   初始化幂等保护。

关键函数：

1. `Init(ClientRoom room)`
   绑定房间与事件，启动已有实体补生成流程。
2. `BootstrapExistingEntities()`
   处理初始化前已到达的同步实体。
3. `OnObjectSpawned(...)` / `OnObjectDestroyed(...)`
   响应对象出生销毁事件。
4. `TrackOrStartSpawn(...)` / `StartResolveAndSpawn(...)`
   异步解析预制体并实例化。
5. `CleanupEntity(...)`
   清理实例、任务与条目状态。

### 10.8 `NetworkMonitoring/Client/Infrastructure/ClientNetworkMonitor.cs`

关键函数：

1. `Init(ClientApp app, INetworkTransport transport)`
   初始化监测依赖并建立与客户端状态的关联。
2. `OnPacketReceived()`
   收包计数入口，参与弱网判定指标。
3. `OnPingResult(...)`
   记录 RTT 结果并更新当前质量状态。
4. `Update()`
   周期计算网络质量并广播 `Local_NetworkQualityChanged`。
